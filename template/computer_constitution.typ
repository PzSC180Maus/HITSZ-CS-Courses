#import "../lib.typ": *
#show: conf

#default-cover(
  title: [计算机组成原理],
  subtitle: [期末复习笔记],
)
#import "@preview/codly:1.3.0": *
#import "@preview/codly-languages:0.1.1": *
#show: codly-init.with()
#show table: set align(center)
#default-outline()

#pagebreak()
= 计算机组成原理概述

#introduction[冯·诺依曼架构][7个伟大思想][性能指标]

== 计算机发展历程

#definition[四代计算机（PPT第4页）][
  #figure(
    table(
      columns: 5,
      table.header[][第一代][第二代][第三代][第四代],
      [年代], [1946~], [1958~], [1965~], [1971~],
      [基础器件], [电子管], [晶体管], [集成电路], [超大规模集成电路],
      [速度], [几千~几万次/s], [几万~几百万次/s], [几十万~几百万次/s], [MIPS→GIPS→TIPS],
      [存储器], [水银延迟线/磁鼓], [磁芯/磁盘/磁带], [半导体/磁盘], [半导体/磁盘],
      [特征], [机器/汇编语言], [FORTRAN/COBOL/OS], [小型机/软件技术], [微型机/网络化],
    ),
    caption: [电子计算机发展历程],
  )

  趋势：速度越来越快、体积越来越小、成本越来越低、功耗越来越低。
]

== 冯·诺依曼架构

#theorem[冯·诺依曼架构的六大特点（PPT第6页）][
  1. 计算机由*五大部件*组成（运算器、控制器、存储器、输入设备、输出设备）
  2. 指令和数据以同等地位存于存储器，可按地址寻访
  3. 指令和数据都用*二进制*表示
  4. 指令由*操作码*和*地址码*组成
  5. *存储程序*：指令在存储器中按顺序存放，通常是顺序执行，特定条件下可改变执行顺序
  6. 以*运算器*为中心
]

=== 冯·诺依曼 vs 哈佛架构

#definition[两种架构对比][
  - *冯·诺依曼（普林斯顿）架构*：程序空间和数据空间*不分开*，指令和数据共用总线。不能同时取指和取操作数。早期X86为代表。
  - *哈佛架构*：程序空间和数据空间*分开*，可同时取指和取操作数。DSP和ARM为代表。

  现代处理器：外部总线是冯·诺依曼结构，但内部由于Cache的存在，实际上已类似*改进型哈佛结构*（数据和指令分开，但总线复用）。
]

==  计算机体系结构中的7个伟大思想

#theorem[7个伟大思想][
  1. *使用抽象简化设计*
  2. *加速经常性事件*（Make the common case fast）
  3. *通过并行提高性能*
  4. *通过流水线提高性能*
  5. *通过预测提高性能*
  6. *存储层次*
  7. *通过冗余提高可靠性*
]

== 计算机硬件的主要指标

=== 时间指标

#definition[核心时间指标][
  - *主频*：CPU内核工作的时钟频率（Hz）
  - *时钟周期*：$T = 1 / f$，一次时钟脉冲的时间
  - *CPI*（Cycles Per Instruction）：平均每条指令所需时钟周期数
  - *IPC*（Instructions Per Cycle）：每时钟周期执行指令数，$op("IPC") = 1 / op("CPI")$
  - *MIPS*（Million Instructions Per Second）：每秒百万条指令
  - *MFLOPS*：每秒百万次浮点运算
  - *CPU执行时间*：$T_"CPU" = "指令数" times op("CPI") times T_"clock"$
  - *响应时间*：从任务开始到完成的总时间
  - *吞吐率*：单位时间内完成的任务数
]

#note[CPU执行时间公式（必记）][
  $T_"CPU" = "IC" times op("CPI") times T_"clock" = ("IC" times op("CPI")) / f$

  IC = Instruction Count（指令总数），$T_"clock" = 1/f$（时钟周期）。
]

=== 非时间指标

#definition[非时间指标][
  - 机器字长：CPU一次能处理的二进制位数
  - 总线宽度：数据总线的位数
  - 主存容量、存储带宽
  - CPU内核数
]

=== 历年真题

#problem[2025春 · 第1题][
  下列关于冯·诺依曼结构的叙述中，错误的是 \_\_\_\_\_\_
  #choices(
  [程序的功能由中央处理器执行指令实现 ],
  [数据都在指令中给出 ],
  [指令和数据都以二进制形式表示],
  [执行前，指令和数据都必须先放在存储器中]
  )
]

#solution[
  答案：*B*。

  B错误：数据*不一定*在指令中给出。数据可以来自寄存器、存储器或立即数。冯·诺依曼架构中指令和数据都存储在存储器中，但数据可以通过寄存器或存储器间接给出。
]

#problem[2025春 · 第三题第1小题（简答题）][
  A、B、C、D 四类指令占比分别为 40%, 20%, 15%, 25%，CPI 分别为 1, 2, 2, 2，CPU 的主频为 500 MHz。现优化部分代码，使 A 类指令的数量变为原来的一半，其他指令数量不变。（计算结果保留两位小数)\

  (1) 计算优化前后 CPI。\
  (2) 若优化前指令总数为 $10^9$，计算优化前后执行时间（单位：秒）。
]

#solution[
  *(1) 优化前 CPI*：
  $op("CPI")_"前" = 0.4 times 1 + 0.2 times 2 + 0.15 times 2 + 0.25 times 2 = 0.4 + 0.4 + 0.3 + 0.5 = 1.6$

  *(2) 优化后*：指令总数变化。设优化前总指令数为 $N = 10^9$。

  优化前：A类 = $0.4N$，其他 = $0.6N$。\
  #h(2em)优化后：A类 = $0.2N$，其他 = $0.6N$。总指令数 = $0.8N$。

  各指令占比：A = $0.2/0.8 = 25%$，B = $0.2/0.8 = 25%$，C = $0.15/0.8 = 18.75%$，D = $0.25/0.8 = 31.25%$

  $op("CPI")_"后" = 0.25 times 1 + 0.25 times 2 + 0.1875 times 2 + 0.3125 times 2 = 0.25 + 0.5 + 0.375 + 0.625 = 1.75$

  *(3) 执行时间*：
  $T_"前" = N times op("CPI") / f = 10^9 times 1.6 / (500 times 10^6) = 1.6 times 2 = 3.20 "s"$
  $T_"后" = 0.8N times 1.75 / (500 times 10^6) = 0.8 times 10^9 times 1.75 / (5 times 10^8) = 0.8 times 1.75 times 2 = 2.80 "s"$
]

#problem[2025春 · 第三题第2小题（简答题）][
  RISC 精简指令集架构指令数量少，因此 CISC 的一条指令的同等功能往往由多条 RISC 指令实现，那为什么 RISC 总体上还能比 CISC 运行更快？
]

#solution[
  主要原因：
  1. *RISC指令定长*（如RISC-V都是32位），硬件设计更规整，便于*流水线*实现。CISC不定长指令难以高效流水。
  2. RISC指令执行时间接近一致（大多1周期），*CPI更小*。CISC复杂指令可能需多周期。
  3. RISC采用*Load/Store架构*，只有访存指令访问内存，其他指令操作寄存器，速度更快。
  4. RISC有大量*通用寄存器*（32个），减少访存次数。
  5. RISC简化了控制器设计（硬布线 vs 微程序），主频可以更高。

  总之：RISC用更多指令数换取更快的单指令速度和更高的主频，总体性能更好。
]

#problem[2019补考 · 填空第1题][
  冯·诺依曼体系都包含哪五个部件？
]

#solution[
  *运算器*、*控制器*、*存储器*、*输入设备*、*输出设备*。
]

== 应试提醒

#conclusion[第一章高频考点][
  #table(
    columns: 3,
    table.header[题型][典型考法][出现年份],
    [选择/填空], [冯·诺依曼架构特点（五大部件/六大特点）], [2025春#1, 2019补#1],
    [选择/填空], [7个伟大思想辨析], [常考],
    [选择/填空], [冯·诺依曼 vs 哈佛架构区别], [常考],
    [计算大题], [CPU执行时间/CPI/MIPS的计算], [2025春 三.1],
    [简答], [RISC vs CISC 为什么RISC更快], [2025春 三.2],
    [填空], [计算机发展四代特征], [常考],
  )
]

#problemset[

  #problem[课堂作业 1：计算 CPI 和 MIPS][
    假设一台计算机主频为 $1 "GHz"$，在其上运行由 $2 times 10^5$ 条指令组成的目标代码。程序主要由 4 类指令组成，它们所占的比例和各自的 CPI 如下表所示，求程序的 $op("CPI")$ 和 $op("MIPS")$。

    #table(
      columns: 3,
      table.header[指令类型][CPI][指令比例],
      [算术和逻辑], [1], [60%],
      [Load/Store], [2], [18%],
      [转移], [4], [12%],
      [Cache缺失访存], [8], [10%],
    )
  ]

  #solution[
    $op("CPI") = 1 times 60% + 2 times 18% + 4 times 12% + 8 times 10% = 2.24$

    $op("MIPS") = f / (op("CPI") times 10^6) = 10^9 / (2.24 times 10^6) = 446.4$
  ]

  #problem[课堂作业 2：计算程序执行时间][
    在上一题的条件下，求该程序的执行时间。
  ]

  #solution[
    $T_"CPU" = "IC" times op("CPI") / f = (2 times 10^5) times 2.24 / 10^9 = 4.48 times 10^(-4) "s" = 0.448 "ms"$

    如果按选择题作答，最接近的选项是 *0.45 ms*。
  ]
]



#pagebreak()
= 计算机的运算方法

#introduction[数的表示][定点运算][浮点运算]

== 计算机中数的表示

=== 无符号数和有符号数

#definition[无符号数][
  寄存器的位数直接反映无符号数的表示范围。
  - 8位寄存器：$0 tilde.op 255$
  - 16位寄存器：$0 tilde.op 65535$
]

#definition[真值与机器数][
  - *真值*：带符号的实际数值，如 $+0.1011$, $-1100$
  - *机器数*：符号数字化的数，符号位 0 表示正，1 表示负
]

=== 原码表示法

#definition[原码定义][
  带符号的绝对值表示。

  *整数原码*（$n$ 为数值位数）：
  $ [x]_"原" = cases(
    0, x quad 2^n > x >= 0,
    2^n - x quad 0 >= x > -2^n
  ) $

  *小数原码*：
  $ [x]_"原" = cases(
    x quad 1 > x >= 0,
    1 - x quad 0 > x > -1
  ) $
]

#example[][
  *例1.* 已知 $[x]_"原" = 1.0011$，求 $x$。\
  *例2.* 已知 $[x]_"原" = 1,1100$，求 $x$。\
  *例3.* 已知 $[x]_"原" = 0.1101$，求 $x$。\
  *例4.* 求 $x = 0$ 的原码。
]

#solution[
  *例1解*：由小数原码定义得 $x = 1 - [x]_"原" = 1 - 1.0011 = -0.0011$

  *例2解*：由整数原码定义得 $x = 2^4 - [x]_"原" = 10000 - 1,1100 = -1100$

  *例3解*：$[x]_"原" = 0.1101$，符号位为 $0$，$ therefore x = +0.1101$

  *例4解*：设 $x = +0.0000$，则 $[+0.0000]_"原" = 0.0000$；设 $x = -0.0000$，则 $[-0.0000]_"原" = 1.0000$。同理整数 $[+0]_"原" = 0,0000$，$[-0]_"原" = 1,0000$。

]

#conclusion[原码的优缺点][
  - 优点：简单、直观
  - 缺点：1) $[+0]_"原" != [-0]_"原"$（有正负零）\ #h(3em)2) 做加法时需判断符号（正+负实际做减法）
]

=== 补码表示法 ★

#definition[补码定义][
  *整数补码*（$n$ 为数值位数，模 $2^(n+1)$）：
  $ [x]_"补" = cases(
    0, x quad 2^n > x >= 0,
    2^(n+1) + x quad 0 > x >= -2^n
  ) $

  *小数补码*（模 $2$）：
  $ [x]_"补" = cases(
    x quad 1 > x >= 0,
    2 + x quad 0 > x >= -1
  ) $
]

#property[补码的核心性质][
  - 正数的补码 = 原码 = 反码
  - 当真值为*负*时，补码可用原码*除符号位外每位取反，末位加1*求得
  - 当真值为负时，已知补码求原码的快捷方法：
    1. 补码除符号位外，每位取反，末位加1
    2. 补码除符号位外，末位减1，再每位取反
]

#example[已知小数补码求真值][
  已知 $[x]_"补" = 1.0001$，求 $x$ 和 $[x]_"原"$。
]

#solution[
  由定义：$x = [x]_"补" - 2 = 1.0001 - 10.0000 = -0.1111$

  由 $x = -0.1111$ 得 $[x]_"原" = 1.1111$

  快捷方法验证：$[x]_"补" = 1.0001$，除符号位外每位取反得 $1.1110$，末位加1得 $[x]_"原" = 1.1111$。
]

#example[已知整数补码求真值][
  已知 $[x]_"补" = 1,1110$，求 $x$。
]

#solution[
  由定义：$x = [x]_"补" - 2^(4+1) = 1,1110 - 100000 = -0010$

  快捷方法：$[x]_"补" = 1,1110$，除符号位外每位取反得 $1,0001$，末位加1得 $[x]_"原" = 1,0010$，$therefore x = -0010$。
]

#example[求下列真值的补码][
  求 $x = 70$（十进制）、$x = -0.1110$、$x = plus.minus 0$、$x = -1.0000$ 的补码。
]

#solution[
  #table(
    columns: 4,
    [真值], [$[x]_"补"$], [$[x]_"原"$], [说明], 
    [$x = 70 = 1000110$], [$0,1000110$], [$0,1000110$], [正数], 
    [$x = -70$], [$1,0111010$], [$1,1000110$], [负数], 
    [$x = -0.1110$], [$1.0010$], [$1.1110$], [], 
    [$x = +0.0000$], [$0.0000$], [$0.0000$], [[+0]补=[-0]补=0], 
    [$x = -0.0000$], [$0.0000$], [$1.0000$], [补码中零唯一], 
    [$x = -1.0000$], [$1.0000$], [不能表示], [[-1]补=2+(-1)=1.0000], 
  )
]

#theorem[补码加减法公式][
  *加法*：$[A]_"补" + [B]_"补" = [A+B]_"补" (op("mod") 2^(n+1))$（整数）
  
  *减法*：$[A-B]_"补" = [A]_"补" + [-B]_"补" (op("mod") 2^(n+1))$（整数）

  连同符号位一起相加，符号位产生的进位自然丢弃。
]

#example[已知 $[y]_"补"$，求 $[-y]_"补"$][
  设 $[y]_"补" = y_0 . y_1 y_2 dots y_n$，根据 $y_0$ 为 0 或 1 分别讨论，推导 $[-y]_"补"$。
]

#solution[
  *情况I*：$[y]_"补" = 0.y_1 y_2 dots y_n$，即 $y$ 为正数。
  $y = 0.y_1 y_2 dots y_n$ $arrow.r$ $-y = -0.y_1 y_2 dots y_n$
  $therefore [-y]_"补" = 1.y_1 y_2 dots y_n + 2^(-n)$ —— 即 $[y]_"补"$ 每位取反，末位加1

  *情况II*：$[y]_"补" = 1.y_1 y_2 dots y_n$，即 $y$ 为负数。
  $[y]_"原" = 1.y_1 y_2 dots y_n + 2^(-n)$ $arrow.r$ $y = -(0.y_1 y_2 dots y_n + 2^(-n))$
  $therefore -y = 0.y_1 y_2 dots y_n + 2^(-n)$ $arrow.r$ $[-y]_"补" = 0.y_1 y_2 dots y_n + 2^(-n)$

  #conclusion[结论][
    $[y]_"补"$ 连同符号位在内，每位取反，末位加1，即得 $[-y]_"补"$。此为高频考点！
  ]
]

=== 反码表示法

#definition[反码定义][
  正数的反码 = 原码；负数的反码 = 原码按位取反（符号位除外）。
]

#example[已知反码求真值][
  已知 $[x]_"反" = 1,1110$，求 $x$。并求 $x=0$ 的反码。
]

#solution[
  $[x]_"反" = 1,1110$：由反码定义得 $x = [x]_"反" - (2^(4+1) - 1) = 1,1110 - 11111 = -0001$

  0的反码：$[+0.0000]_"反" = 0.0000$，$[-0.0000]_"反" = 1.1111$。同理整数 $[+0]_"反" = 0,0000$，$[-0]_"反" = 1,1111$。
  $arrow.r [+0]_"反" != [-0]_"反"$
]

#conclusion[三种机器数小结][
  - 对于正数：符号位为0，原码 = 补码 = 反码
  - 对于负数：原码取反 $arrow.r$ 反码 $arrow.r$ 末位加1 $arrow.r$ 补码
  - $[+0]_"原" != [-0]_"原"$，$[+0]_"反" != [-0]_"反"$，$[+0]_"补" = [-0]_"补" = 0$
]

=== 移码表示法

#definition[移码定义][
  $ [x]_"移" = 2^n + x quad (2^n > x >= -2^n) $
  
  移码 = 补码的符号位取反。主要用于浮点数的阶码，便于比较大小。
]

#example[移码计算][
  $x = 10100$（真值），求 $[x]_"移"$ 和 $[x]_"补"$。
]

#solution[
  设 $n=5$：$[x]_"移" = 2^5 + 10100 = 1,10100$
  $[x]_"补" = 0,10100$（正数的补码=原码）
  验证：$[x]_"补"$ 符号位取反 $= 1,10100 = [x]_"移"$
]

#figure(
  table(
    columns: 6,
    table.header[真值 $x$ $($n=5$)$][$[x]_"补"$][$[x]_"移"$][对应的十进制整数],
    [$-100000$], [$100000$], [$000000$], [$0$],
    [$-11111$], [$100001$], [$000001$], [$1$],
    [$-00001$], [$111111$], [$011111$], [$31$],
    [$plus.minus 00000$], [$000000$], [$100000$], [$32$],
    [$+00001$], [$000001$], [$100001$], [$33$],
    [$+11110$], [$011110$], [$111110$], [$62$],
    [$+11111$], [$011111$], [$111111$], [$63$],
  ),
  caption: [真值、补码和移码的对照表 ($n=5$)],
)

=== 8位机器数的表示范围

#problem[机器数的真值][
  设机器数字长为8位（其中1位为符号位），对于整数，当其分别代表无符号数、原码、补码和反码时，对应的真值范围各为多少？
]

#figure(
  table(
    columns: 5,
    table.header[二进制代码][无符号数真值][原码真值][补码真值][反码真值],
    [$00000000$], [$0$], [$+0$], [$+0$], [$+0$],
    [$00000001$], [$1$], [$+1$], [$+1$], [$+1$],
    [$00000010$], [$2$], [$+2$], [$+2$], [$+2$],
    [$dots$], [$dots$], [$dots$], [$dots$], [$dots$],
    [$01111111$], [$127$], [$+127$], [$+127$], [$+127$],
    [$10000000$], [$128$], [$-0$], [$-128$], [$-127$],
    [$10000001$], [$129$], [$-1$], [$-127$], [$-126$],
    [$dots$], [$dots$], [$dots$], [$dots$], [$dots$],
    [$11111101$], [$253$], [$-125$], [$-3$], [$-2$],
    [$11111110$], [$254$], [$-126$], [$-2$], [$-1$],
    [$11111111$], [$255$], [$-127$], [$-1$], [$-0$],
  ),
  caption: [8位机器数（1位符号位+7位数值位）真值范围对照（PPT第26页）],
)

#conclusion[表示范围总结][
  - 原码小数：$-(1 - 2^(-n)) tilde.op (1 - 2^(-n))$
  - 补码小数：$-1 tilde.op (1 - 2^(-n))$（多表示一个 $-1$）
  - 补码整数：$-2^n tilde.op (2^n - 1)$（多表示一个 $-2^n$，如 8 位时 $-128$）
]

=== 定点表示和浮点表示

#definition[定点数][
  小数点位置固定。
  - 小数定点机：$S_f S_1 S_2 dots S_n$（小数点在最左侧）
  - 整数定点机：$S_f S_1 S_2 dots S_n$（小数点在最右侧）
]

#definition[浮点数][
  一般形式：$N = S times r^j$
  - $S$：尾数（可正可负，决定*精度*）
  - $j$：阶码（可正可负，决定*范围*）
  - $r$：基数（计算机中通常为 $2$）
]

#definition[浮点数的机器格式][
  $underbrace(j_f j_1 j_2 dots j_m, "阶码") underbrace(S_f S_1 S_2 dots S_n, "尾数")$
  
  - $j_f$：阶符；$m$ 位阶码数值位，反映表示范围
  - $S_f$：数符；$n$ 位尾数数值位，反映精度
]


=== 二进制数规格化

#definition[规格化数定义][
  基数 $r = 2$ 时：$1/2 <= |S| < 1$
  
  即：尾数的绝对值在 $1/2$ 到 $1$ 之间（不含 $1$）。
]

#figure(
  table(
    columns: 4,
    table.header[][正数规格化形式][负数规格化形式][判断规则],
    [真值], [$0.1 dots$], [$-0.1 dots$], [],
    [原码], [$0.1 dots$], [$1.1 dots$], [第一数位为1],
    [补码], [$0.1 dots$], [$1.0 dots$], [符号位和第一数位不同],
    [反码], [$0.1 dots$], [$1.0 dots$], [],
  ),
  caption: [规格化数的判断规则],
)

#remark[
  - $[-1/2]_"补" = 1.100 dots 0$：定义是规格化，但机器判断不是（特例）
  - $[-1]_"补" = 1.000 dots 0$：定义不是规格化，但机器判断是（特例）
]

#definition[机器零][
  - 尾数为 0 时，不论阶码为何值
  - 阶码小于它所能表示的最小值时，不论尾数为何值
  
  当阶码用移码、尾数用补码时：$0, 0000; 0.00 dots 0$。便于电路中"判0"。
]

=== IEEE 754 浮点数标准 ★★★

#definition[单精度（32-bit）][
  1 位符号 $S$ + 8 位指数 $E$（偏移 $127$）+ 23 位尾数 $M$

  换算：$(-1)^S times (1 + M_2) times 2^(E - 127)$
]

#definition[双精度（64-bit）][
  1 位符号 $S$ + 11 位指数 $E$（偏移 $1023$）+ 52 位尾数 $M$

  换算：$(-1)^S times (1 + M_2) times 2^(E - 1023)$
]

#figure(
  table(
    columns: 3,
    table.header[指数 $E$][尾数 $M$][表示对象],
    [$0$], [$0$], [$plus.minus 0$（符号位决定正负）],
    [$0$], [$!= 0$], [非规格化数 $(-1)^S times M_2 times 2^(1-"bias")$],
    [$1 tilde 254$], [任意], [规格化浮点数 $(-1)^S times (1+M_2) times 2^(E-"bias")$],
    [$255$], [$0$], [$plus.minus infinity$（无穷大）],
    [$255$], [$!= 0$], [NaN（非数）],
  ),
  caption: [IEEE 754 单精度特殊值编码],
)

#example[IEEE 754 单精度浮点数计算实例][
  $A = 1.5$（十进制），$B = 0.5 + 2^(-24)$（十进制），计算 $C = A + B$ 的 IEEE 754 单精度表示。
]

#solution[
  *IEEE 754 表示*：
  - $A = 0#h(0.2em) 01111111#h(0.2em) 10000000000000000000000$（指数 $E_a = 127$，真实指数 $0$）
  - $B = 0#h(0.2em) 01111110#h(0.2em) 00000000000000000000001$（指数 $E_b = 126$，真实指数 $-1$）

  *Step 1 对阶*：$E_a - E_b = 1 > 0$，所以 $B$ 向 $A$ 看齐。
  - 原始尾数：$1.000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0001$
  - 右移 1 位：$0.100#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 1$
  - 舍入后：$0.100#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0001#h(0.2em) 0$
  - 指数调整为 $126 + 1 = 127$，与 $A$ 相同

  *Step 2 尾数相加*：
  - $A$ 尾数：$1.100#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000$
  - $B$ 尾数：$0.100#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0001$（已对阶 + 舍入）
  - 中间和：$10.000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0001$

  *Step 3 右规 + 舍入*：
  - 右移 1 位：$1.0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 000#h(0.2em) 1$
  - 二次舍入：$1.0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 0000#h(0.2em) 001$

  *最终结果*：$0#h(0.2em) 10000000#h(0.2em) 00000000000000000000001$（指数 $127 + 1 = 128$）
  $arrow.r$ $1.00000000000000000000001 times 2^1 = 2 + 2^(-22)$

  #note[关键发现][
    精确结果应为 $2 + 2^(-24)$，但由于对阶和舍入误差，实际得到 $2 + 2^(-22)$。*结合律失效*：$(a+b)+c != a+(b+c)$。
  ]
]

#note[浮点数实际影响][
  *经典问题：为什么 $0.1 + 0.2 != 0.3$？*
  - 根本原因：十进制小数转二进制产生无限循环
  - 对阶、舍入加剧表示误差

  *建议*：
  - 实际比较应使用容差：$op("abs")(a - b) < epsilon$
  - 理解浮点数的精度限制
  - 避免等值比较，使用范围比较
  - 金融等精确计算使用十进制类型
]

=== 算术移位与逻辑移位

#definition[算术移位][
  有符号数的移位。符号位不变！
]

#figure(
  table(
    columns: 4,
    table.header[真值][机器数表示][左移空位补][右移空位补],
    [正数], [原码/补码/反码], [$0$], [$0$],
    [负数], [原码], [$0$], [$0$],
    [负数], [补码], [$0$], [*$1$*],
    [负数], [反码], [$1$], [$1$],
  ),
  caption: [算术移位规则总结],
)

#definition[逻辑移位][
  无符号数的移位。逻辑左移/右移：空位一律补 $0$。
]

#example[移位对比][
  补码表示的机器数 $10110010$：
  - 逻辑左移 $= 01100100$，逻辑右移 $= 01011001$
  - 算术左移 $= 11100100$，算术右移 $= 11011001$
]

#figure(
  image("../assets/computer_constitution/ch2_算术移位-031.png", width: 85%),
  caption: [真值，补码，移码对照表],
)

== 定点运算 ★★★

=== 补码加减法

#theorem[补码加减公式][
  整数：
  $ [A+B]_"补" = [A]_"补" + [B]_"补" (op("mod") 2^(n+1)) $
  $ [A-B]_"补" = [A]_"补" + [-B]_"补" (op("mod") 2^(n+1)) $

  小数：
  $ [A+B]_"补" = [A]_"补" + [B]_"补" (op("mod") 2) $
  $ [A-B]_"补" = [A]_"补" + [-B]_"补" (op("mod") 2) $

  核心：减法变加法，连同符号位一起运算！
]

=== 原码一位乘法

#definition[原码乘法规则][
  乘积符号 $= X_"符号" xor Y_"符号"$（异或），数值部分按绝对值相乘。
  用逻辑移位，移位的次数判断乘法是否结束。
]

=== 补码一位乘法（Booth 算法）★★★

#theorem[Booth 算法核心公式（小数）（PPT第103页）][
  设 $[x]_"补" = x_0 . x_1 x_2 dots x_n$，$[y]_"补" = y_0 . y_1 y_2 dots y_n$，附加位 $y_(n+1) = 0$

  $ [x times y]_"补" = [x]_"补"[(y_1 - y_0) + (y_2 - y_1)2^(-1) + dots + (y_(n+1) - y_n)2^(-n)] $

  递推公式：$[z_0]_"补" = 0$，$[z_i]_"补" = 2^(-1){(y_(n+2-i) - y_(n+1-i))[x]_"补" + [z_(i-1)]_"补"}$
  最后一步不移位！
]

#figure(
  table(
    columns: 4,
    table.header[$y_i$][$y_(i+1)$][$y_(i+1) - y_i$][操作],
    [$0$], [$0$], [$0$], [部分积 $arrow.r 1$（仅移位）],
    [$0$], [$1$], [$+1$], [部分积 $+$ $[x]_"补"$ $arrow.r 1$],
    [$1$], [$0$], [$-1$], [部分积 $+$ $[-x]_"补"$ $arrow.r 1$],
    [$1$], [$1$], [$0$], [部分积 $arrow.r 1$（仅移位）],
  ),
  caption: [Booth 算法操作规则（*必记！*）],
)

#example[Booth 算法完整演练（小数）][
  已知 $x = +0.0011$，$y = -0.1011$，用 Booth 算法求 $[x times y]_"补"$。
]

#solution[
  $[x]_"补" = 0.0011$，$[y]_"补" = 1.0101$，$[-x]_"补" = 1.1101$，附加位 $y_4 = 0$

  #table(
    columns: 4,
    [*部分积*], [*乘数 + 附加位*], [*$y_i y_(i+1)$*], [*操作*],
    [$0.0000$], [$1.0101 0$], [$y_0 y_1 = 10$], [$+[-x]_"补"$],
    [$+1.1101$], [], [], [], 
    [$1.1101$], [$10101$], [], [$arrow.r 1$（算术右移）], 
    [$1.1110 1$], [$1010 10$], [$y_1 y_2 = 01$], [$+[x]_"补"$], 
    [$+0.0011$], [], [], [], 
    [$0.0001$], [$11010$], [], [$arrow.r 1$], 
    [$0.0000 11$], [$101 010$], [$y_2 y_3 = 10$], [$+[-x]_"补"$], 
    [$+1.1101$], [], [], [], 
    [$1.1101$], [$11101$], [], [$arrow.r 1$], 
    [$1.1110 111$], [$10 1010$], [$y_3 y_4 = 01$], [$+[x]_"补"$], 
    [$+0.0011$], [], [], [],
    [$0.0001$], [$11110$], [], [$arrow.r 1$], 
    [$0.0000 1111$], [$1 01010$], [$y_4 y_5 = 10$], [$+[-x]_"补"$，最后一步不移位], 
    [$+1.1101$], [], [], [], 
    [$1.1101 1111$], [], [], [],
  )

  #conclusion[最终结果][
    $ [x times y]_"补" = 1.1101 1111 $
  ]
]

== 2.3 浮点运算 ★★★

=== 浮点加减运算步骤

#theorem[浮点加减流程][
  *五步法*：对阶 $arrow.r$ 尾数求和 $arrow.r$ 规格化（左规/右规） $arrow.r$ 舍入 $arrow.r$ 溢出判断

  1. *对阶*：小阶向大阶看齐。$Delta j = j_x - j_y$，阶差为负则 $x$ 向 $y$ 看齐，阶差为正则 $y$ 向 $x$ 看齐
  2. *尾数求和*：对阶后的两个尾数相加/减
  3. *规格化*：
     - 原码：不论正数负数，第一数位为1
     - 补码：符号位和第一数位不同
     - 左规：尾数左移直到满足规格化，阶码相应减
     - 右规：尾数溢出（双符号位 $01$ 或 $10$）时右移1位，阶码加1
  4. *舍入*：0舍1入法或恒置1法
  5. *溢出判断*：看阶码是否 $01, dots$（上溢）或 $10, dots$（下溢）
]

#example[浮点加法][
  $x = 0.1101 times 2^(01)$，$y = (-0.1010) times 2^(11)$，求 $x + y$（阶码取2位，尾数取4位）。
]

#solution[
  $[x]_"补" = 00,01; 00.1101$，$[y]_"补" = 00,11; 11.0110$

  *Step 1 对阶*：
  $[Delta j]_"补" = [j_x]_"补" - [j_y]_"补" = 00,01 - 00,11 = 00,01 + 11,01 = 11,10$（$-2$）
  阶差为负 $arrow.r$ $x$ 向 $y$ 看齐，$S_x$ 右移2位：
  $[x]_"补"' = 00,11; 00.0011$

  *Step 2 尾数求和*：
  $[S_x]_"补"' + [S_y]_"补" = 00.0011 + 11.0110 = 11.1001$
  $arrow.r$ $[x+y]_"补" = 00,11; 11.1001$

  *Step 3 左规*：符号位 $1$，第一数位 $1$（相同）$arrow.r$ 需左规
  左移1位：$[x+y]_"补" = 00,10; 11.0010$

  #conclusion[最终结果][
    $x + y = (-0.1110) times 2^(10)$
  ]
]

#example[浮点减法（含右规）（PPT第128页延续）][
  $x = (-5/8) times 2^(-5)$，$y = (7/8) times 2^(-4)$，求 $x - y$（阶码取3位，尾数取6位）。
]

#solution[
  $x = (-0.101000) times 2^(-101)$，$y = (0.111000) times 2^(-100)$
  
  $[x]_"补" = 11,011; 11.011000$，$[y]_"补" = 11,100; 00.111000$

  *Step 1 对阶*：
  $[Delta j]_"补" = 11,011 - 11,100 = 11,011 + 00,100 = 11,111$（$-1$）
  $arrow.r$ $x$ 向 $y$ 看齐：$[x]_"补"' = 11,100; 11.101100$

  *Step 2 尾数求差*：
  $[S_x]_"补"' + [-S_y]_"补" = 11.101100 + 11.001000 = 110.110100$
  （双符号位：$10.dots$，溢出！）

  *Step 3 右规*：右移1位，阶码加1
  $[x-y]_"补" = 11,101; 11.011010$

  #conclusion[最终结果][
    $x - y = (-0.100110) times 2^(-11) = (-19/32) times 2^(-3)$
  ]
]

=== 溢出判断

#definition[浮点数溢出][
  设阶符取2位，阶码数值部分取7位，数符取2位，尾数取 $n$ 位：
  - 阶码 $01, dots$ $arrow.r$ *上溢*（阶码大于最大阶码 $127$）
  - 阶码 $10, dots$ $arrow.r$ *下溢*（阶码小于最小阶码 $-128$，按机器零处理）
]

#note[考试注意事项][
  - 浮点运算步骤*必须*写全：对阶 $arrow.r$ 求和 $arrow.r$ 规格化 $arrow.r$ 舍入 $arrow.r$ 溢出判断
  - 对阶一定小阶向大阶看齐
  - 补码判断规格化：符号位 $!= $ 第一数位
  - Booth 算法最后一步*不移位*
  - IEEE 754 规格化数尾数前面隐含 $1$
  - 浮点数运算不满足结合律
]

=== 应试速查卡

#conclusion[第二章核心公式速查][
  #table(
    columns: 2,
    table.header[问题][答案],
    [$[x]_"补" -> [-x]_"补"$], [连同符号位取反 $+ 1$],
    [负数补码 $->$ 原码], [除符号位外每位取反 $+ 1$],
    [补码加减], [连同符号位运算，进位丢弃],
    [IEEE 754 单精度偏移], [$127$],
    [IEEE 754 双精度偏移], [$1023$],
    [规格化数 $r=2$], [$1/2 <= |S| < 1$],
    [补码规格化判断], [符号位和第一数位不同],
    [Booth 最后一步], [不移位],
    [浮点对阶原则], [小阶向大阶看齐],
    [右规条件], [双符号位 $01$ 或 $10$],
    [机器零], [尾数为 $0$ 或阶码小于最小值],
  )
]

== 历年真题选编

#introduction[选择题][填空题][简答/计算题]

=== 选择题

#problem[2025春 · 第2题][
  在以下编码中，+0 和 -0 的编码相同的是 \_\_\_\_\_\_ \
  I. 原码 #h(2em) II. 反码 #h(2em) III. 补码 #h(2em) IV. 移码

  #choices([I, II],[II, III],[III, IV],[I, IV])
]

#solution[
  答案：*C*（III, IV）。

  解析：$[+0]_"原" = 0.0000$，$[-0]_"原" = 1.0000$，不同。$[+0]_"反" = 0.0000$，$[-0]_"反" = 1.1111$，不同。$[+0]_"补" = [-0]_"补" = 0.0000$，相同。$[+0]_"移" = [-0]_"移" = 100dots 0$，相同。
]

#problem[2019补考 · 第1题][
  补码规格化时，符号位和小数部分应 \_\_\_\_\_\_

  #choices(
    [符号位和小数首位一致],
    [符号位和小数末位一致],
    [符号位和小数首位不一致],
    [符号位和小数末位不一致],
  )
]

#solution[
  答案：*C*（符号位和小数首位不一致）。

  解析：补码规格化数的判断规则：符号位和第一数位（小数首位）不同。正数规格化补码为 $0.1 dots$，负数规格化补码为 $1.0 dots$。
]

#problem[2025春 · 第6题][
  对数字 BAH，算术右移一位，算术左移一位，逻辑右移一位，得到的结果分别是 \_\_\_\_\_\_ \

  #choices(
    [DDH, 74H, 5DH],
    [5DH, 75H, F5H],
    [DDH, 75H, F4H],
    [5DH, 74H, DDH],
  )
]

#solution[
  答案：*A*（DDH, 74H, 5DH）。

  解析：BAH = 1011 1010B（补码表示的负数）。
  - *算术右移一位*：符号位补1，低位丢弃。1011 1010 $arrow.r$ 1101 1101 = DDH。
  - *算术左移一位*：符号位保持不变，左移后可能溢出截断。1011 1010 $arrow.r$ (1)0111 0100 = 74H（左移时产生溢出，符号位被丢弃，实质上是发生了上溢）。
  - *逻辑右移一位*：高位补0。1011 1010 $arrow.r$ 0101 1101 = 5DH。

  #note[考点][
    补码的算术移位：右移符号位补1，左移符号位不变（注意溢出情况）。逻辑移位一律补0。
  ]
]

=== 填空题

#problem[2025春 · 填空第1题][
  16 位浮点数要表示 $-6 times 10^4 tilde.op +6 times 10^4$ 的数，在保证精度最高的条件下，除了一位阶符，阶码应取 \_\_\_\_\_\_ 位；除了一位数符，尾数应取 \_\_\_\_\_\_ 位。
]

#solution[
  解析：$6 times 10^4 approx 2^16$（$2^16 = 65536$），因此阶码至少需表示 $16$。

  设阶码数值位为 $m$，则最大阶码 $= 2^m - 1$，需 $2^m - 1 >= 16$。当 $m = 5$ 时 $2^5 - 1 = 31 >= 16$，满足。

  16 位总长：阶符1位 + 阶码数值5位 + 数符1位 + 尾数数值 $= 16$，故尾数数值位 $= 9$。

  答案：阶码取 *5* 位，尾数取 *9* 位。
]

#problem[2025春 · 填空第2题][
  -320 用 IEEE 754 的单精度表示，其十六进制表示为 \_\_\_\_\_\_。
]

#solution[
  *Step 1*：$-320$ 为负数，符号位 $S = 1$。

  *Step 2*：$320 = 256 + 64 = 2^8 + 2^6$，$320 = 101000000_2$（二进制）。

  *Step 3*：规格化：$320 = 1.01 times 2^8$，尾数 $M$（23位）= $010000dots 0$（即 1.01 去掉隐含的 1）。

  *Step 4*：指数 $E = 8 + 127 = 135 = 10000111_2$。

  *Step 5*：组合 $S E M$：
  $1\ 10000111\ 01000000000000000000000$

  分组为十六进制：
  $1100 0011 1010 0000 0000 0000 0000 0000$
  $= op("C3A00000")_16$

  答案：*C3A00000*。

  #note[验证][
    也可直接计算：$-320 = -1.25 times 2^8$（$320/256 = 1.25$），$M = 0.25 = 0100dots 0_2$，结果一致。
  ]
]

#problem[2019补考 · 填空第2题][
  一个 16 位数据（包含一位符号位），它的原码、反码、补码、移码表示分别是什么？
]

#solution[
  16位数据（1位符号 + 15位数值）：

  #table(
    columns: 4,
    table.header[范围类型][原码][反码][补码],
    [最大正数], [$(2^15 - 1)$], [$(2^15 - 1)$], [$(2^15 - 1)$],
    [最小正数], [$+1$], [$+1$], [$+1$],
    [+0], [$0$], [$0$], [$0$],
    [-0], [$2^15$], [$2^16 - 1$], [$0$（与+0相同）],
    [最大负数], [$-1$], [$-(2^15 - 1)$], [$-1$],
    [最小负数], [$-(2^15 - 1)$], [$-(2^15 - 1)$], [$-2^15$],
  )

  #conclusion[核心结论][
    - 原码表示范围：$-(2^15 - 1) tilde.op +(2^15 - 1)$，有正负零
    - 补码表示范围：$-2^15 tilde.op +(2^15 - 1)$，零唯一，多表示一个 $-2^15$
    - 移码 = 补码符号位取反，用于浮点比较
  ]
]

=== 简答 / 计算题

#problem[2025春 · 第四题 计算题][
  设 $X = -256\/5$，$Y = -1024\/59$ (\/ 后的数字表示进制)，使用浮点数补码计算 $X - Y$，阶符取 2 位，阶码取 3 位，尾数取 9 位（忽略对阶及尾数舍入）。求结果的二进制表示。
]

#solution[
  *Step 1*：转换为二进制并写成浮点形式。

  $-256 = -2^8$，$X = -2^8 / 5 = -51.2$。$51 = 110011_2$，$0.2 times 2 = 0.4 arrow.r 0$，$0.4 times 2 = 0.8 arrow.r 0$，$0.8 times 2 = 1.6 arrow.r 1$，$0.6 times 2 = 1.2 arrow.r 1$，$0.2 times 2 = 0.4 arrow.r 0$（循环）。
  
  $51.2_10 = 110011.00110011dots_2$（无限循环小数 $0011$）。

  规格化：$X = -1.100110011 dots times 2^5$（指数 $5$）。

  $Y = -1024/59$：$-1024 = -2^10$。$1024/59 approx -17.3559dots$
  $17 = 10001_2$，小数部分也需要转换（此处简化计算）。

  实际上，这道题的关键是：先将 X 和 Y 分别用补码浮点数表示（阶符2位+阶码3位+数符+尾数9位），然后对阶、尾数相减、规格化。

  *Step 2*：X 的浮点补码表示。

  $X = -110011.00110011dots_2 = -1.100110011 dots times 2^5$
  阶码真值 = $+5 = 00101$，阶符=0，阶码=0101 → 阶码补码表示：00,0101
  尾数 $S_X = -1.100110011$（9位）：$[S_X]_"补" = 1.011001101$（取反加一得）

  故 $[X]_"补" = 00,0101; 1.011001101$

  *Step 3*：Y 的浮点补码表示。

  $Y = -10001.010110dots_2$（$1024/59 approx 17.356$）。$17 = 10001_2$，小数点后精度取9位。
  $Y approx -1.000101011 dots times 2^4$
  阶码真值 = $+4$，补码：00,0100
  $[S_Y]_"补" = 1.111010101$（近似）

  *Step 4*：计算 $X - Y = X + (-Y)$。

  对阶：$j_X - j_Y = 5 - 4 = 1$，Y 向 X 看齐。$[S_Y]_"补"'$ 右移1位。
  $[S_Y]_"补"' = 1.111101011$，阶码调整为 00,0101

  尾数相减：$[S_X]_"补" + [-S_Y]_"补"$（此处继续计算 $arrow.r$ 最终得到二进制浮点补码结果）

  #note[说明][
    此题涉及浮点数补码加减的完整流程（对阶→尾数运算→规格化），是考试大题出题方向。完整计算过程较长，考场上需注意阶符/阶码/尾数位数限制，以及对阶和规格化操作。
  ]
]

#problem[2019补考 · 简答第1题][
  用 IEEE 754 标准表示出 -5.0（十进制），把结果表示为十六进制。
]

#solution[
  *Step 1*：$-5.0$ 为负数，符号位 $S = 1$。

  *Step 2*：$5.0 = 101.0_2 = 1.01 times 2^2$，尾数 $M$（23位）= $01000 dots 0$。

  *Step 3*：指数 $E = 2 + 127 = 129 = 10000001_2$。

  *Step 4*：组合 $S E M$：
  $1#h(0.2em) 10000001#h(0.2em) 01000000000000000000000$

  分组为十六进制：
  $1100 0000 1010 0000 0000 0000 0000 0000$
  $= op("C0A00000")_16$

  答案：*C0A00000*。
]

#problem[2019补考 · 简答第8题][
  两个尾数有四位的二进制小数 $x$ 和 $y$，都是负数，要用 Booth 算法求出 $[x times y]_"补"$（具体数值回忆版未给出）。
]

#solution[
  *典型解题步骤*：
  1. 写出 $[x]_"补"$ 和 $[y]_"补"$，计算 $[-x]_"补"$（连同符号位取反+1）
  2. 设置 $[z_0]_"补" = 0$，附加位 $y_(n+1) = 0$
  3. 从低到高考察 $(y_i, y_{i+1})$，按规则操作：
     - 00 或 11：仅算术右移
     - 01：$+[x]_"补"$，再算术右移
     - 10：$+[-x]_"补"$，再算术右移
  4. 重复 n+1 次，最后一步不移位

]

=== 应试提醒

#conclusion[第二章高频考题类型][
  #table(
    columns: 3,
    table.header[题型][典型考法][出现年份],
    [选择题], [+0/-0编码是否相同], [2025春],
    [选择题], [补码规格化判断], [2019补考],
    [选择题], [算术/逻辑移位计算], [2025春],
    [填空题], [浮点数阶码/尾数位数分配], [2025春],
    [填空题], [IEEE 754单精度转换（十六进制）], [2025春、2019补考],
    [计算大题], [浮点数补码加减（对阶/规格化）], [2025春],
    [计算大题], [Booth算法（两个负数小数）], [2019补考],
    [简答题], [16位数据原反补移码表示范围], [2019补考],
  )
]

#pagebreak()
= RISC-V汇编及其指令系统

#introduction[RISC-V概述][汇编语言与伪指令][六种指令格式][案例分析]

== RISC-V概述

=== ISA基本概念

#definition[ISA（指令集体系结构）][
  ISA是软件子系统和硬件子系统的*抽象接口层*，包含了程序员正确编写二进制机器语言程序所需的全部信息。
]

#definition[指令集架构（ISA）位宽][
  ISA位宽 = 通用寄存器的宽度，决定寻址范围和数据运算能力。

  *注意*：ISA位宽 $!= $ 指令编码长度。即便64位架构也大量使用16位编码的指令。
]

#definition[系列机][
  基本指令系统相同、基本系统结构相同的计算机。解决软件兼容问题。
  IBM 360 是第一个将ISA与其实现分离的系列机。
]

=== 硬件设计四原则

#theorem[硬件设计四原则][
  1. *简单性来自规则性*（Simplicity favors regularity）
  2. *越小越快*（Smaller is faster）
  3. *加快经常性事件*（Make the common case fast）
  4. *好的设计需要适度折衷*（Good design demands good compromises）
]

=== RISC-V寄存器集

#definition[RV32I通用寄存器][
  32个通用寄存器：x0 tilde.op x31。*x0* 恒为 0（特殊寄存器）。

  每个寄存器有别名便于软件使用，实际硬件并无区别：

  #align(center,
  [#table(
    columns: 4,
    table.header[寄存器][ABI名称][用途][保存者],
    [x0], [zero], [恒为0], [--],
    [x1], [ra], [返回地址], [调用者],
    [x2], [sp], [栈指针], [被调用者],
    [x3], [gp], [全局指针], [--],
    [x4], [tp], [线程指针], [--],
    [x5-x7], [t0-t2], [临时寄存器], [调用者],
    [x8], [s0/fp], [帧指针/保存寄存器], [被调用者],
    [x9], [s1], [保存寄存器], [被调用者],
    [x10-x17], [a0-a7], [函数参数/返回值], [调用者],
    [x18-x27], [s2-s11], [保存寄存器], [被调用者],
    [x28-x31], [t3-t6], [临时寄存器], [调用者],
  )])
]


== 3.2 RISC-V汇编语言

=== 汇编指令格式

#definition[RISC-V通用指令格式][
  `op dst, src1, src2`

  - 1个操作码，3个操作数
  - 每条指令只有一个操作，每行最多一条指令
  - 操作的数据必须直接来自*寄存器*
]

=== 伪指令（高频考点）

#figure(
  table(
    columns: 3,
    table.header[伪指令][实际展开指令][说明],
    [`li rd, imm`], [`addi rd, x0, imm`], [加载立即数],
    [`mv rd, rs`], [`addi rd, rs, 0`], [寄存器传送],
    [`j label`], [`jal x0, label`], [无条件跳转],
    [`ret`], [`jalr x0, 0(x1)`], [函数返回],
    [`nop`], [`addi x0, x0, 0`], [空操作],
    [`not rd, rs`], [`xori rd, rs, -1`], [按位取反],
    [`beqz rs, label`], [`beq rs, x0, label`], [等于零跳转],
    [`bnez rs, label`], [`bne rs, x0, label`], [不等零跳转],
    [`la rd, symbol`], [`auipc rd, ...` + `addi rd, ...`], [加载地址],
  ),
  caption: [RISC-V常用伪指令对照表（*必记！*）],
)

=== 常用RISC-V指令分类

#definition[算术/逻辑指令（R型）][
  `add/sub/and/or/xor/sll/srl/sra/slt/sltu rd, rs1, rs2`

  opcode = 0110011，由 funct3 + funct7 决定具体操作。
]

#definition[立即数指令（I型）][
  `addi/andi/ori/xori rd, rs1, imm`

  `slli/srli/srai rd, rs1, shamt`（移位立即数，shamt 只有 5/6 位）
]

#definition[访存指令][
  - Load: `lw/ld/lb/lh/lbu/lhu rd, offset(rs1)`（I型，opcode=0000011）
  - Store: `sw/sd/sb/sh rs2, offset(rs1)`（S型，opcode=0100011）

  RISC-V 是小端序，不支持字不对齐访问（lw/sw 地址需4字节对齐）。
]

#definition[分支指令（B型）][
  `beq/bne/blt/bge/bltu/bgeu rs1, rs2, label`

  opcode = 1100011。分支目标地址 = PC + 立即数 $times 2$（B型立即数隐含最低位为0）。

  注意：RISC-V 没有条件码（如 x86 的 ZF/CF），而是用寄存器比较 + 分支。
]

#definition[跳转指令][
  - `jal rd, label`：J型，rd = PC+4，PC = PC + offset。rd=x0 即无条件跳转（j伪指令）
  - `jalr rd, offset(rs1)`：I型，rd = PC+4，PC = rs1 + offset。常用于函数返回
]

=== 函数调用约定

#property[调用者 vs 被调用者保存][
  - *调用者保存*：ra(x1), t0-t6(x5-x7,x28-x31), a0-a7(x10-x17)
  - *被调用者保存*：sp(x2), s0-s11(x8-x9,x18-x27)

  调用者保存 = 函数调用前，由*调用者*负责保存到栈中。
  被调用者保存 = 函数内部使用前，由*被调用者*负责保存/恢复。
]

#definition[栈操作模式][
  栈向下增长（sp 减小）。入栈：`addi sp, sp, -N` + `sd`。出栈：`ld` + `addi sp, sp, N`。

  叶过程（leaf procedure）：不调用其他函数的过程（如 swap），*不需要保存 ra*。
  非叶过程（如 sort 内部调用 swap）：*必须保存 ra* 到栈中。
]

== 3.3 RISC-V指令表示 ★★

=== 六种指令格式

#figure(
  image("../assets/computer_constitution/ch3_六种指令格式-08.png", width: 90%),
  caption: [RISC-V六种指令格式字段布局],
)

#definition[六种格式速查][
  #table(
    columns: 3,
    table.header[格式][用途][字段特征],
    [*R型*], [寄存器-寄存器操作], [funct7(7) | rs2(5) | rs1(5) | funct3(3) | rd(5) | opcode(7)],
    [*I型*], [立即数 + Load], [imm[11:0](12) | rs1(5) | funct3(3) | rd(5) | opcode(7)],
    [*S型*], [Store], [imm[11:5](7) | rs2(5) | rs1(5) | funct3(3) | imm[4:0](5) | opcode(7)],
    [*B型*], [条件分支], [imm[12|10:5](7) | rs2(5) | rs1(5) | funct3(3) | imm[4:1|11](5) | opcode(7)],
    [*U型*], [LUI/AUIPC], [imm[31:12](20) | rd(5) | opcode(7)],
    [*J型*], [JAL], [imm[20|10:1|11|19:12](20) | rd(5) | opcode(7)],
  )
]

#note[关键对比][
  - R型有 rs2（源寄存器2），I型无rs2有12位立即数
  - S型将12位立即数拆成 [11:5] 和 [4:0] 两部分
  - B型立即数按1位单位编码（分支目标总是偶数地址，最低位隐含0）
  - U型/J型有20位立即数，U型直接 [31:12]，J型分散编码
]

=== 汇编→机器码转换 ★

#example[add指令转机器码][
  将 `add x18, x19, x10` 转换为机器码。
]

#solution[
  R型：opcode = 0110011, funct3 = 000, funct7 = 0000000

  rs2 = x10 = 01010, rs1 = x19 = 10011, rd = x18 = 10010

  结果：`0000000 01010 10011 000 10010 0110011`

  十六进制：`0x00A9 8933`。
]


== 3.4 案例分析 ★★

=== swap 过程（叶过程）

#example[swap的C代码和汇编实现][
  ```c
  void swap(long long int v[], long long int k) {
      long long int temp;
      temp = v[k];
      v[k] = v[k+1];
      v[k+1] = temp;
  }
  ```
  v $arrow.r$ x10, k $arrow.r$ x11, temp $arrow.r$ x5
]

#solution[
  ```asm
  swap:   slli x6, x11, 3    # x6 = k * 8 (左移3位=乘8)
          add  x6, x10, x6   # x6 = v + (k * 8)
          ld   x5, 0(x6)     # temp = v[k]
          ld   x7, 8(x6)     # x7 = v[k+1]
          sd   x7, 0(x6)     # v[k] = x7
          sd   x5, 8(x6)     # v[k+1] = temp
          jalr x0, 0(x1)     # return (ret伪指令)
  ```

  注意：swap 是*叶过程*，不调用其他函数，因此无需保存 ra 到栈中。
]

=== sort 过程（调用者）

#example[sort（插入排序）汇编实现][
  ```c
  void sort(long long int v[], int n) {
      int i, j;
      for (i = 1; i < n; i++)
          for (j = i-1; j >= 0 && v[j] > v[j+1]; j--)
              swap(v, j);
  }
  ```
  v $arrow.r$ x10, n $arrow.r$ x11, i $arrow.r$ x19, j $arrow.r$ x20

  sort 调用 swap，需保存 ra 和 s 寄存器。
]

#solution[
  完整汇编代码框架：

  *1. 保存寄存器（入栈）*：
  ```asm
  sort:   addi sp, sp, -40   # 5个双字空间
          sd   x1, 32(sp)    # ra（因sort调用swap）
          sd   x22, 24(sp)   # x22 = n的副本
          sd   x21, 16(sp)   # x21 = v的副本
          sd   x20, 8(sp)    # x20 = j
          sd   x19, 0(sp)    # x19 = i
  ```

  *2. 参数复制*：
  ```asm
          mv   x21, x10      # 保存v到x21（swap用x10传参）
          mv   x22, x11      # 保存n到x22
          li   x19, 1        # i = 1
  ```

  *3. 外层循环*：
  ```asm
  for1tst:bge  x19, x22, exit1  # i >= n → 退出
          addi x20, x19, -1      # j = i - 1
  ```

  *4. 内层循环（含swap调用）*：
  ```asm
  for2tst:blt  x20, x0, exit2   # j < 0 → 退出
          slli x5, x20, 3        # x5 = j * 8
          add  x5, x21, x5       # x5 = v + j*8
          ld   x6, 0(x5)         # x6 = v[j]
          ld   x7, 8(x5)         # x7 = v[j+1]
          ble  x6, x7, exit2     # v[j] <= v[j+1] → 退出
          mv   x10, x21          # swap参数1: v
          mv   x11, x20          # swap参数2: j
          jal  x1, swap          # 调用swap
          addi x20, x20, -1      # j--
          j    for2tst           # 继续内循环
  ```

  *5. 恢复寄存器（出栈）*：
  ```asm
  exit1:  ld   x19, 0(sp)
          ld   x20, 8(sp)
          ld   x21, 16(sp)
          ld   x22, 24(sp)
          ld   x1, 32(sp)
          addi sp, sp, 40
          jalr x0, 0(x1)    # return
  ```
]

=== 数组 vs 指针

#example[数组下标清零 vs 指针清零][
  对比两种清零方式的汇编实现。
]

#solution[
  *数组下标方式*：
  ```asm
  # array->x10, size->x11, i->x5
         li   x5, 0            # i = 0
  loop1: slli x6, x5, 3        # x6 = i * 8
         add  x7, x10, x6      # x7 = &array[i]
         sd   x0, 0(x7)        # array[i] = 0
         addi x5, x5, 1        # i++
         blt  x5, x11, loop1   # if i < size, loop
  ```

  *指针方式*：
  ```asm
  # array->x10, size->x11, p->x5
         mv   x5, x10          # p = &array[0]
         slli x6, x11, 3       # x6 = size * 8
         add  x7, x10, x6      # x7 = &array[size]
  loop2: sd   x0, 0(x5)        # *p = 0
         addi x5, x5, 8        # p++（地址+8）
         bltu x5, x7, loop2    # if p < &array[size], loop
  ```

  #conclusion[比较][
    数组方式乘法在循环内（`slli x6, x5, 3`），指针方式只需每次 +8。但编译器可自动优化，推荐用下标更清晰安全。
  ]
]

== 历年真题选编

=== 选择题

#problem[2025春 · 第3题][
  `auipc rd, imm` 指令的含义是 \_\_\_\_\_\_

  #choices(
    [向给定寄存器中载入给定的立即数],
    [向给定寄存器中载入当前程序计数器与给定立即数的和],
    [跳转到给定的立即数地址并将当前程序计数器的值写入给定寄存器],
    [以上说法都不正确],
  )
]

#solution[
  答案：*B*。

  AUIPC = Add Upper Immediate to PC。将 PC + (imm << 12) 结果写入 rd。常用于构造 PC 相对地址，配合 addi 实现 `la` 伪指令。
]

=== 程序分析题

#problem[2025春 · 第五题第1小题（8分）][
  阅读以下 RISC-V 汇编程序并回答问题：

  ```asm
  .data
  arr:    .word 1,2,3,4,5

  .text
  .globl _start
  _start: li   x5, 5
          li   x6, 0
          la   x8, arr
          li   x7, 0
  loop:   beq  x5, x0, end
          lw   x9, 0(x8)
          add  x7, x7, x9
          addi x8, x8, 4
          addi x5, x5, -1
          bnez x5, loop
  end:    nop
  ```

  (1) 执行完毕后，寄存器 x7 的值是 \_\_\_\_\_\_。（2分）
  (2) 将伪指令 `bnez x5, loop` 改写为等效的真实指令 \_\_\_\_\_\_。（2分）
]

#solution[
  (1) 程序功能：遍历数组 arr = [1,2,3,4,5]，累加到 x7。每次 `lw x9, 0(x8)` 加载一个元素，x7 累加，x8 地址 +4（word=4字节），x5 递减到0。最终 x7 = 1+2+3+4+5 = *15*。

  (2) `bnez x5, loop` $arrow.r$ `bne x5, x0, loop`

  #note[关键考点][
    - `la` 伪指令用于加载标签地址
    - `.word` 声明 32位 数据（4字节），所以每次地址 +4
    - x5 既是循环计数器（从5递减），也是 beq 的比较对象
  ]
]

#problem[2025春 · 第五题第2小题（6分）][
  阅读以下代码：

  ```asm
  loop:      beq  x10, x0, exit_loop
             addi x10, x10, -1
             addi x11, x11, 1
             j    loop
  exit_loop:
  ```

  (1) 若 x10 初值 = 3，x11 初值 = 0，执行结束后 x11 = \_\_\_\_\_\_。（3分）
  (2) 若 x10 初值 = N，则该代码片段共执行了 \_\_\_\_\_\_ 条指令。（3分）
]

#solution[
  (1) 循环体（addi addi j）执行 3 次（x10=3,2,1），beq 执行 4 次（x10=3,2,1,0）。x11 = 0 + 3 = *3*。

  (2) 循环体（3条）执行 N 次，beq 执行 N+1 次。共计 $3N + (N+1) = *4N+1*$ 条指令。

  注意：当 x10=0 时 beq 成立直接跳到 exit_loop，循环体不再执行。
]

#problem[2019补考 · 简答第10题][
  C 语言翻译成 RISC-V 汇编代码（回忆版未给出具体C代码）。
]

#solution[
  典型翻译模式（必考）：

  - `if (a == b)` $arrow.r$ `beq ra, rb, label`
  - `while (i < n)` $arrow.r$ `loop: bge ri, rn, exit; ...; j loop`
  - `f(a, b)` $arrow.r$ `mv a0, ra; mv a1, rb; jal ra, f`
  - `a = b + c` $arrow.r$ `add ra, rb, rc`
  - `a = arr[i]`（双字）$arrow.r$ `slli t, ri, 3; add t, rarr, t; ld ra, 0(t)`
  - `a = arr[i]`（字）$arrow.r$ `slli t, ri, 2; add t, rarr, t; lw ra, 0(t)`

  关键：区分 word（lw/sw，$i times 4$）和 doubleword（ld/sd，$i times 8$）。
]

#problem[2019补考 · 选择第7题（大端小端）][
  一串数据 12345678H，存放在内存地址 0x4000 到 0x4003 上，每个存储单元可以放入一个字节，在小端条件下，0x4001 存放着什么数据？
]

#solution[
  *小端序（Little-Endian）*：低位字节存放在低地址。

  数据 12345678H 从低到高字节：78H, 56H, 34H, 12H。

  地址分配：
  - 0x4000: 78H（最低字节）
  - 0x4001: *56H*
  - 0x4002: 34H
  - 0x4003: 12H（最高字节）

  答案：0x4001 存放 *56H*。

  #note[大端 vs 小端][
    - *小端序*（RISC-V、x86）：低地址存低字节。便于类型转换，`&int` 直接当 `&char` 用。
    - *大端序*（网络字节序）：低地址存高字节。更符合人类阅读习惯。
    - RISC-V 默认小端序，但也支持大端序变体。
  ]
]

=== 第三章应试提醒

#conclusion[第三章高频考题类型][
  #table(
    columns: 3,
    table.header[题型][典型考法][出现年份],
    [选择题], [伪指令展开（li/mv/j/nop等）], [常考],
    [选择题], [auipc指令含义], [2025春],
    [填空题], [汇编指令→机器码转换], [常考],
    [程序分析], [阅读汇编代码求寄存器最终值], [2025春 五.1/五.2],
    [程序分析], [C代码→汇编代码翻译], [2019补],
    [案例分析], [函数调用栈帧（入栈/出栈寄存器）], [常考],
    [案例分析], [调用者/被调用者保存寄存器识别], [常考],
  )
]

#pagebreak()
= 处理器（CPU）設計

#introduction[数据通路组件][控制器设计][单周期CPU]

== 处理器设计概述

=== CPU两大组成部分

#definition[数据通路（Datapath）][
  处理器中*执行所需操作*的硬件，执行控制器的命令。$approx$ 处理器的四肢。
  例如：控制器说"执行add"，数据通路就把操作数交给ALU做加法。
]

#definition[控制器（Controller）][
  对数据通路进行*调度*的硬件。$approx$ 处理器的大脑。
  决定：执行什么操作？源操作数选哪个？结果写哪里？PC如何更新？
]


=== 冯诺依曼 vs 哈佛架构

#definition[两种架构对比][
  - *冯诺依曼（普林斯顿）架构*：程序空间和数据空间*一体*，指令和数据共用总线。不能同时取指和取操作数（X86为代表）。
  - *哈佛架构*：程序存储器和数据存储器*分开*，可同时取指和取操作数，效率高（DSP、ARM为代表）。
  - RISC-V CPU 采用*哈佛架构*：指令存储器 IM 和数据存储器 DM 分开。
]

== 4.2 数据通路组件

=== 关键组件

#definition[数据通路核心组件][
  #table(
    columns: 3,
    table.header[组件][功能][类型],
    [*PC（程序计数器）*], [存放下一条指令地址], [时序逻辑（寄存器）],
    [*指令存储器 IM*], [存放指令，只读], [组合逻辑],
    [*寄存器堆（Register File）*], [32个通用寄存器，可同时读2个写1个], [时序逻辑],
    [*ALU*], [算术/逻辑运算], [组合逻辑],
    [*数据存储器 DM*], [读写数据], [时序逻辑（写需时钟）],
    [*立即数生成器*], [从指令字段提取/扩展立即数], [组合逻辑],
    [*多路选择器（MUX）*], [从多个输入中选择一个输出], [组合逻辑],
    [*加法器*], [PC+4 或 分支目标地址计算], [组合逻辑],
  )
]

#figure(
  image("../assets/computer_constitution/ch4_CPU组成-005.png", width: 85%),
  caption: [CPU两大组成部分：控制器 + 数据通路],
)

== 各类指令的数据通路

=== 所有指令的共同需求
#definition[指令执行的共同数据通路需求][
  1. PC $arrow.r$ IM $arrow.r$ 取指令
  2. 更新 PC （PC + 4 或 分支目标地址）
]
#figure(
  image("../assets/computer_constitution/all_instruct_presequite.png", width: 85%),
  caption: "所有指令的共同需求"
)
=== R型指令（add/sub/and/or/slt）

#definition[R型指令数据通路][
  1. PC $arrow.r$ IM $arrow.r$ 取指令
  2. 读 rs1, rs2 寄存器
  3. ALU 运算（由 ALUOp 控制）
  4. 结果写回 rd 寄存器

  控制信号：RegWrite=1, ALUSrc=0（第二个操作数来自寄存器）, MemtoReg=0（写回来自ALU）
]

=== I型算术指令（addi/andi/ori）

#definition[I型算术数据通路][
  与R型类似，区别：第二个操作数来自*立即数*而非 rs2 寄存器。

  控制信号：ALUSrc=1（选择立即数）, RegWrite=1, MemtoReg=0
]

=== Load指令（lw/ld）

#definition[Load数据通路][
  1. 读 rs1 + 立即数 $arrow.r$ ALU 计算存储器地址
  2. 用计算出的地址读 DM
  3. DM 输出写回 rd

  控制信号：ALUSrc=1, MemRead=1, MemtoReg=1（写回来自存储器）, RegWrite=1
]

=== Store指令（sw/sd）

#definition[Store数据通路][
  1. 读 rs2（要存的数据）+ rs1 + 立即数
  2. ALU 计算存储器地址
  3. 将 rs2 的值写入 DM

  控制信号：ALUSrc=1, MemWrite=1, RegWrite=0（不写寄存器）
]

=== Branch指令（beq/bne）

#definition[Branch数据通路][
  1. 读 rs1, rs2 $arrow.r$ ALU 做减法比较
  2. Zero 信号指示是否相等
  3. 若满足分支条件：PC $arrow.r$ PC + 立即数$times 2$

  控制信号：ALUSrc=0, Branch=1（配合 Zero 决定是否跳转）, RegWrite=0
]


== 4.4 控制器设计

=== 控制信号汇总

#figure(
  table(
    columns: 2,
    table.header[控制信号][功能],
    [*RegWrite*], [是否写寄存器堆（1=写）],
    [*ALUSrc*], [ALU第二个操作数来源（0=rs2寄存器, 1=立即数）],
    [*MemRead*], [是否读数据存储器],
    [*MemWrite*], [是否写数据存储器],
    [*MemtoReg*], [写回寄存器的数据来源（0=ALU结果, 1=DM输出）],
    [*Branch*], [是否分支跳转],
    [*ALUOp*], [ALU运算类型控制（配合funct3/funct7）],
  ),
  caption: [RISC-V单周期控制器关键信号],
)

=== 各类指令的控制信号取值

#figure(
  table(
    columns: 8,
    table.header[指令类型][RegWrite][ALUSrc][MemRead][MemWrite][MemtoReg][Branch][ALUOp],
    [R型（add/sub）], [$1$], [$0$], [$0$], [$0$], [$0$], [$0$], [由funct决定],
    [I型（addi）], [$1$], [$1$], [$0$], [$0$], [$0$], [$0$], [add],
    [Load（lw）], [$1$], [$1$], [$1$], [$0$], [$1$], [$0$], [add],
    [Store（sw）], [$0$], [$1$], [$0$], [$1$], [$X$], [$0$], [add],
    [Branch（beq）], [$0$], [$0$], [$0$], [$0$], [$X$], [$1$], [sub],
  ),
  caption: [各类RISC-V指令的控制信号取值（*必记*）],
)

=== 单周期设计的性能

#property[单周期CPU特点][
  - *优点*：设计简单，每条指令一个时钟周期完成
  - *缺点*：时钟周期由*最慢指令*决定（通常为load指令：取指$arrow.r$读寄存器$arrow.r$ALU$arrow.r$访存$arrow.r$写寄存器，路径最长）
  - *违反设计原则*：无法加速经常性事件
  - *解决方案*：流水线（第5章）
]

=== 历年真题

#problem[2019补考 · 简答第11题][
  数据通路题目：给出一条 lw 指令和一条 R 型指令，要求：
  (1) 写出两条指令对应的控制信号
  (2) 判断两条指令是否使用了 IM 和 DM
  (3) 若执行 lw 时 MemRW 从 read 错误地变为 write，会导致什么后果？
]

#solution[
  (1) lw：RegWrite=1, ALUSrc=1, MemRead=1, MemWrite=0, MemtoReg=1, Branch=0。R型：RegWrite=1, ALUSrc=0, MemRead=0, MemWrite=0, MemtoReg=0, Branch=0。

  (2) IM：两条指令*都用到*（取指令阶段必须读IM）。DM：只有 lw 用到（读数据），R型指令*不用*DM。

  (3) lw 目的是从 DM 读取数据。若 MemRW 错误地变为 write，会将寄存器的值错误地写入 DM 中 lw 计算出的地址处，导致：
  - 原 DM 地址的数据被覆盖/破坏
  - lw 读回的不是期望的存储器数据，后续程序出错
]

== 应试提醒

#conclusion[第四章高频考点][
  #table(
    columns: 3,
    table.header[题型][典型考法][出现年份],
    [概念], [冯诺依曼 vs 哈佛架构区别], [常考],
    [概念], [控制器 vs 数据通路的功能划分], [常考],
    [填空/简答], [某指令的控制信号填写], [2019补],
    [分析], [各类指令在数据通路中的执行路径], [常考],
    [分析], [单周期CPU关键路径（load最长）], [常考],
  )
]

#pagebreak()
= 流水线处理器

#introduction[流水线概念][数据冒险与控制冒险][例外处理]

== 流水线概述

=== 为什么要流水线

#property[单周期CPU的问题][
  - 时钟周期由最慢指令（load）决定，$T_"clock" = 800$ps
  - 每条指令执行时间相同，无法加速常见指令
  - 违反"加速经常性事件"的原则
]

=== 洗衣比喻

#definition[流水线加速原理][
  洗衣四步（各0.5h）：洗衣 $arrow.r$ 烘干 $arrow.r$ 叠衣 $arrow.r$ 收纳

  - 串行：4件衣服 = 8h
  - 流水：4件衣服 = 3.5h，加速比 = 2.3
  - 大量负载：加速比 $approx$ 流水线级数 = 4

  流水线*不减少单件时间*，但通过*重叠执行*提高吞吐率。
]


=== RISC-V五级流水线

#definition[五个阶段][
  #table(
    columns: 3,
    table.header[阶段][全称][功能],
    [*IF*], [Instruction Fetch], [从IM取指令],
    [*ID*], [Instruction Decode], [读寄存器 + 指令译码],
    [*EX*], [Execute], [ALU运算 / 计算地址],
    [*MEM*], [Memory Access], [读/写数据存储器],
    [*WB*], [Write Back], [结果写回寄存器],
  )
]

=== 流水线性能

#definition[各阶段耗时示例][
  #table(
    columns: 7,
    table.header[指令类型][IF][ID][EX][MEM][WB][总时间],
    [ld], [200ps], [100ps], [200ps], [200ps], [100ps], [*800ps*],
    [sd], [200ps], [100ps], [200ps], [200ps], [--], [700ps],
    [R型], [200ps], [100ps], [200ps], [--], [100ps], [600ps],
    [beq], [200ps], [100ps], [200ps], [--], [--], [500ps],
  )

  - 单周期：$T_"clock" = 800$ps，CPI = 1
  - 五级流水：$T_"clock" = 200$ps（由最慢阶段决定），理想 CPI $approx$ 1
  - 加速比 $approx$ 4
]

#note[流水线性能计算][
  流水线周期 = $op("max")("各阶段耗时")$，不是各阶段耗时的加和。加速比 $approx$ 流水线级数（理想情况，忽略冒险和气泡）。
]

== 数据冒险：前递与停顿 ★★

=== 三类冒险

#definition[流水线三类冒险][
  - *结构冒险*：硬件资源冲突（如单存储器同时被IF和MEM访问）
  - *数据冒险*：指令间数据依赖（RAW读后写、WAR写后读、WAW写后写）
  - *控制冒险*：分支/跳转导致PC不确定
]

=== 前递（Forwarding）

#definition[前递（旁路 Bypassing）][
  将 EX/MEM 或 MEM/WB 流水线寄存器中的结果直接"前递"给 EX 阶段的 ALU 输入端，无需等到 WB 阶段写回。

  可解决大部分 RAW 冒险。*不是所有情况都能前递解决*。
]

=== 停顿（Stall）

#definition[停顿（插入气泡）][
  *load-use冒险*：load指令后紧跟使用该数据的指令时，即使前递也无法完全解决，需插入一个气泡（stall一个周期）。

  实现方式：保持IF/ID寄存器不变，将ID/EX的控制信号清零（插入nop）。
]

#figure(
  image("../assets/computer_constitution/ch5_数据冒险-17.png", width: 85%),
  caption: [数据冒险的前递与停顿示意],
)

== 控制冒险

#definition[控制冒险的来源][
  分支指令在 EX 阶段才确定是否跳转，但此时已有两条指令进入 IF 和 ID 阶段。若预测失败，需*冲刷*（flush）这些指令。
]

#definition[解决方法][
  - *静态预测*：始终预测不跳转（简单，但准确率低）
  - *动态预测*：分支预测缓冲器（BHT），记录历史跳转行为
  - *分支延迟槽*：分支指令后紧跟一条总是执行的指令（编译器填充）
  - *提前分支判断*：将分支判断移到 ID 阶段，减少惩罚
]

== 例外与中断

#definition[例外 vs 中断][
  - *例外（Exception）*：CPU内部引起（未定义指令、算术溢出、系统调用等）
  - *中断（Interrupt）*：外部事件引起（I/O请求、定时器等）

  RISC-V 通过 CSR（控制状态寄存器）保存例外原因和返回地址。
]

=== 历年真题

#problem[2025春 · 第五题第3小题（6分）][
  基于经典五级流水线 RISC-V 处理器，初始 PC = 36。指令序列：

  ```
  36: sub  x10, x10, x8
  40: beq  x1,  x3, label
  44: jal  x1,  label
  48: addi x5,  x0, 10
  52: sw   x5,  0(x2)
  56: lw   x6,  0(x2)
  60: add  x7,  x6, x5
  64: beq  x7,  x5, label
  ```

  在第三时钟周期：
  (1) IF阶段执行\_\_\_指令；ID阶段执行\_\_\_指令；EX阶段执行\_\_\_指令
  (2) IF阶段 IM 的输出（十六进制）
  (3) 立即数生成器输出
  (4) 加法器输出
  (5) 若 R[x10]=10, R[x8]=8，ALU输入和输出
]

#solution[
  *第三时钟周期*（每条指令1个周期前进1阶段）：

  - Cycle 1: IF(sub), --, --
  - Cycle 2: IF(beq), ID(sub), --
  - Cycle 3: IF(jal), ID(beq), EX(sub)

  (1) IF: *jal*（PC=44）；ID: *beq*（PC=40）；EX: *sub*（PC=36）

  (2) IM输出 = jal 指令的机器码。`jal x1, label`，label = 72（从指令序列可知），offset = 72-44 = 28。J型编码：imm=28, rd=x1, opcode=1101111。值取决于立即数编码方式。

  (3) 立即数生成器输出：ID阶段是 beq 指令，其立即数 = (label - 40)/2（需知道 label 地址）。

  (4) 加法器输出：PC+4 = 44+4 = 48（用于计算下一条指令地址）。

  (5) sub x10, x10, x8：R[x10]=10, R[x8]=8。ALU输入：10 和 8（来自 ID/EX 流水线寄存器）。输出：10-8 = *2*。
]

#problem[2019补考 · 简答第9题][
  流水线中有哪几类冒险？给几行RISC-V汇编代码，判断出现了哪种冒险。
]

#solution[
  三类冒险：结构冒险、数据冒险（RAW/WAR/WAW）、控制冒险。

  典型判断：
  ```asm
  add x1, x2, x3   # x1 = x2 + x3
  sub x4, x1, x5   # 用到x1 → RAW数据冒险（可通过前递解决）
  ```
  ```asm
  lw  x1, 0(x2)    # load x1
  add x4, x1, x5   # 用到x1 → load-use冒险（需停顿1周期）
  ```
  ```asm
  beq x1, x2, L1   # 分支 → 控制冒险（需预测+可能冲刷）
  add x3, x4, x5   # 若分支跳转，这条指令将被冲刷
  ```
]

== 应试提醒

#conclusion[第五章高频考点][
  #table(
    columns: 3,
    table.header[题型][典型考法][出现年份],
    [计算], [流水线加速比/执行时间计算], [常考],
    [概念], [三类冒险的定义与区分], [2019补],
    [分析], [给定指令序列，分析某周期各阶段状态], [2025春 五.3],
    [分析], [判断数据冒险类型及解决方法（前递/停顿）], [常考],
    [概念], [load-use冒险必须停顿], [常考],
    [概念], [例外 vs 中断的区别], [常考],
  )
]

#pagebreak()
= 存储器

#introduction[存储器分类与层次][Cache映射][虚拟存储器]

== 存储器概述

=== 存储器分类

#definition[按存储介质分类][
  - 半导体存储器（TTL/MOS，*易失*）
  - 磁表面存储器（磁头+载磁体）
  - 光盘存储器（激光+磁光材料）
]

#definition[按存取方式分类][
  - *随机访问*：存取时间与物理地址无关（RAM/ROM）
  - *串行访问*：存取时间与物理地址有关（磁带/磁盘）
]

#definition[按在计算机中的作用分类][
  #table(
    columns: 3,
    table.header[层次][类型][特点],
    [CPU内部], [寄存器], [最快，最小],
    [Cache], [SRAM], [快，贵],
    [主存], [DRAM], [容量大，需刷新],
    [辅助存储器], [磁盘/SSD/光盘], [容量最大，最慢，非易失],
  )
]

=== 存储层次结构


#figure(
  image("../assets/computer_constitution/ch6_存储层次-006.png", width: 85%),
  caption: [存储器的层次结构],
)

== 主存储器

=== RAM和ROM

#definition[RAM（随机存储器）][
  - *SRAM*（静态RAM）：速度快，功耗大，6个晶体管/位，用于Cache
  - *DRAM*（动态RAM）：集成度高，需定期刷新，1个晶体管+1个电容/位，用于主存
]

=== DRAM刷新方式 ★

#definition[DRAM刷新原理][
  DRAM 使用电容存储电荷，电荷会逐渐泄漏，必须*定期刷新*才能保持数据不丢失。

  刷新周期：通常为 *2ms*，即每隔 2ms 必须对所有行刷新一遍。
]

#definition[三种刷新方式][
  1. *集中刷新*：在一个刷新周期内，集中一段时间对所有行进行刷新。\ 刷新期间不能进行读写操作，存在*死区*。
  2. *分散刷新*：将刷新操作分散到每个存取周期中。\ 无死区，但刷新频率高，系统速度降低。
  3. *异步刷新*（透明刷新）：利用 CPU 不访问存储器的死时间进行刷新。\ 兼顾两者优点，是现代 DRAM 的常用方式。
]

#example[刷新间隔计算][
  对于 $64 times 64$ 的 DRAM 存储矩阵，共有 *64 行*，最大刷新间隔为 $2 "ms"$。

  *异步刷新*时，需要在 $2 "ms"$ 内将全部 64 行各刷新一遍。

  刷新一行的时间间隔 = $2 "ms" \/ 64 = 31.25 "μs"$。
]

#note[考试提醒][
  - *同步刷新* 不是标准的 DRAM 刷新方式（是干扰项）。
  - 集中刷新有死区；分散刷新无死区但速度慢；异步刷新最常用。
]

#definition[ROM（只读存储器）][
  MROM（掩膜）$arrow.r$ PROM（可编程一次）$arrow.r$ EPROM（紫外线擦除）$arrow.r$ EEPROM（电擦除）$arrow.r$ Flash Memory（U盘/SSD）
]



#figure(
  table(
  columns: 3,
  table.header([对比项], [*SRAM*], [*DRAM*]),
  [存储元件], [双稳态触发器（6管/位）], [电容 + 1管/位],
  [速度], [*快*], [慢],
  [功耗], [*高*], [低],
  [集成度], [低], [*高*],
  [是否需要刷新], [*否*], [*是*（约2ms一次）],
  [成本], [高], [低],
  [典型用途], [Cache], [主存],
),
caption: "SRAM与DRAM特点"
)


=== 存储器扩展

#definition[三种扩展方式][
  - *位扩展*：增加数据位宽（芯片并联，地址线共用）
  - *字扩展*：增加存储容量（地址线增加，片选控制）
  - *字位同时扩展*：同时增加数据位宽和容量
]



#problem[第四次雨课堂 · 主观题第1题（CPU与存储芯片连接设计）][
  设CPU共有16根地址线，8根数据线，并用 #text[MREQ]（低电平有效）作访存控制信号，R/W作读/写命令信号（高电平为读，低电平为写）。

  现有这些存储芯片：ROM（2K×8位，4K×4位，8K×8位），RAM（1K×4位，2K×8位，4K×8位）及74138译码器和其他门电路（门电路自定）。

  试从上述规格中选用合适的芯片，画出CPU和存储芯片的连接图。要求如下：

  1. 最小4K地址为系统程序区，4096~16383地址范围为用户程序区。
  2. 指出选用的存储芯片类型及数量。
  3. 详细画出片选逻辑。
]

#solution[
  *分析地址空间*：

  CPU 地址线 16 根 → 可寻址 $2^16 = 64K$ 字（按字节寻址则为 64KB）。

  - 系统程序区：最小 4K 地址 = $text("0000H") tilde.op text("0FFFH")$（共 $2^12 = 4096$ 字节）
  - 用户程序区：$4096 tilde.op 16383$ = $text("1000H") tilde.op text("3FFFH")$（共 12KB）

  *芯片选型*：

  - 系统程序区（4KB，只读）→ 选用 *2片 4K×4位 ROM*，做位扩展拼成 4K×8位。
  - 用户程序区（12KB，读写）→ 选用 *3片 4K×8位 RAM*，做字扩展覆盖 12KB 空间。

  *片选逻辑设计*：

  地址线分析（$A_0 tilde.op A_15$）：
  - $A_0 tilde.op A_11$：片内寻址（4K = $2^12$）
  - $A_12, A_13, A_14$：接 74138 译码器输入端（$C, B, A$）
  - $A_15$：可用作片选使能控制

  74138 译码器：
  - 输入：$G_1 = A_15$, $G_(2A) = text("MREQ")$, $G_(2B) = 0$（接地）
  - $C = A_14, B = A_13, A = A_12$
  - $Y_0$（对应 $A_14 A_13 A_12 = 000$）：选中系统程序区 ROM（4K）
  - $Y_1, Y_2, Y_3$（对应 001/010/011）：分别选中3片 RAM（各4K）

  *连接图说明*：

  #figure(
    image("../assets/computer_constitution/cpu_design.svg", width: 85%),
    caption: [CPU与存储芯片连接设计图],
  )

  关键点：
  1. ROM 芯片：2片 4K×4位，数据线并联成 8位；$text("CS")$ 接 $Y_0$
  2. RAM 芯片：3片 4K×8位，每片 $text("CS")$ 分别接 $Y_1, Y_2, Y_3$
  3. $text("R/W")$ 信号接 RAM 的 $text("WE")$ 端（写使能）
  4. $text("MREQ")$ 接 74138 的 $G_(2A)$（低电平有效）
]

== 高速缓冲存储器（Cache）★

=== 局部性原理

#theorem[Cache存在的理论基础][
  - *时间局部性*：刚访问过的数据可能再次被访问
  - *空间局部性*：刚访问地址附近的数据可能被访问
]

=== Cache映射方式

#definition[三种映射方式][
  #table(
    columns: 3,
    table.header[方式][映射规则][优缺点],
    [*直接映射*], [每个主存块固定映射到唯一Cache行], [硬件简单，冲突率高],
    [*全相联映射*], [主存块可映射到任意Cache行], [灵活，硬件复杂，比较器多],
    [*组相联映射*], [组间直接映射，组内全相联], [折中方案，最常用],
  )
]

#figure(
  image("../assets/computer_constitution/ch6_Cache映射-038.png", width: 80%),
  caption: [Cache映射方式示意],
)

=== Cache替换算法与写策略

#definition[替换算法][
  当Cache满需要替换时：LRU（最近最少使用）、FIFO（先进先出）、随机。
  LRU效果最好，但硬件实现代价最高。
]

#definition[写策略][
  - *写直达（Write-through）*：同时写Cache和主存（简单，但慢）
  - *写回（Write-back）*：仅写Cache，被替换时才写回主存（快，但需脏位）

  写不命中时的处理：
  - 写分配（Write-allocate）：先将块调入Cache再写
  - 非写分配（No-write-allocate）：直接写主存，不调入Cache
]

#problem[第四次雨课堂][
  设某机主存容量为16MB，按字节寻址，其中Cache容量为16KB，每字块有8个字，每字32位，设计一个四路组相联映射（即Cache每组内共有4个字块）的Cache组织。

  + 画出主存地址字段中各段的位数。
  + 设Cache的初态为空，CPU依次从主存第0,1,2,...,89号单元读出90个字（主存一次读出一个字），并重复按此次序读8次，问命中率是多少？
  + 若Cache的速度是主存的5倍，试问有Cache和无Cache相比，速度约提高多少倍？
  + 地址为ACDBFEH的主存单元会被映射到Cache中的第几组（用十进制表示，设起始组号为第0组）？
]

#solution[
  *（1）主存地址字段划分*

  主存容量 16MB，按字节寻址 → 地址共 *24 位*。

  - 每字 32 位 = 4 字节；每块 8 字 → 块大小 = $8 times 4 = 32$ 字节
  - 块内偏移：$log_2 32 = 5$ 位
  - Cache 容量 16KB，总块数 = $16K / 32 = 512$ 块
  - 四路组相联：组数 = $512 / 4 = 128$ 组 → 组索引 = $log_2 128 = 7$ 位
  - Tag 位数 = $24 - 7 - 5 = 12$ 位


  #figure(
    image("../assets/computer_constitution/for_way_cache.svg", width: 80%),
    caption: [四路组相联Cache地址格式],
  )

  *（2）命中率计算*

  - 主存第0~89号单元共 90 个字 = $90 times 4 = 360$ 字节
  - 块大小 32 字节 → 360 字节跨越 $ceil(360/32) = 12$ 个块
  - 第一次遍历：12 个块全部未命中（Cache初态为空），随后这12个块被装入Cache
  - 第2~8次遍历（共7次）：这12个块全部命中

  总访问次数 = $90 times 8 = 720$
  命中次数 = $90 times 7 = 630$
  命中率 = $630 / 720 = *87.5%*$

  *（3）速度提高倍数*

  设主存存取时间为 $T_M$，则 Cache 存取时间为 $T_C = T_M / 5$。

  - 无Cache时总时间：$T_"无" = 720 T_M$
  - 有Cache时总时间：$T_"有" = 90 T_M + 630 times (T_M/5) = 90 T_M + 126 T_M = 216 T_M$

  加速比 = $720 / 216 = 10/3 approx 3.33$
  速度提高倍数 = $3.33 - 1 = *2.33 倍*$

  *（4）地址 ACDBFEH 映射的组号*

  $"ACDBFEH" = 1010 1100 1101 1011 1111 1110_2$

  地址共24位，取中间7位（组索引，位11~5）：
  ```
  位号：  23 22 ... 11 10  9  8  7  6  5  4 ... 0
  二进制： 1  0  1  0  1  1  0  1  1  1  1  1  1  1  0
  ```

  组索引（位11~5）：$101 1111_2 = 95_(10)$

  答案：映射到 *第95组*。
]

#pagebreak()

#problem[第四次雨课堂 ][
  设主存容量为256K字，Cache容量为2K字，块长为4个字，每字32位，按字寻址，则：

  + 在直接映射方式下，设计主存地址格式。
  + 在四路组相联映射方式下，设计主存地址格式。
  + 在全相联映射方式下，设计主存地址格式。
]

#solution[
  *地址总位数*：主存容量 256K 字 = $2^18$ 字 → 地址共 *18 位*（按字寻址）。

  *块内偏移*：块长 4 字 → 块内偏移 = $log_2 4 = 2$ 位。

  *（1）直接映射*

  Cache 容量 2K 字，总块数 = $2K / 4 = 512$ 块 → 行索引（Cache行号）= $log_2 512 = 9$ 位。

  Tag 位数 = $18 - 9 - 2 = 7$ 位。


  *（2）四路组相联映射*

  512 个Cache块按 4 个一组 → 组数 = $512 / 4 = 128$ 组 → 组索引 = $log_2 128 = 7$ 位。

  Tag 位数 = $18 - 7 - 2 = 9$ 位。


  *（3）全相联映射*

  全相联无需组索引，整个地址（除块内偏移外）均为 Tag。

  Tag 位数 = $18 - 2 = 16$ 位。


  #table(
    columns: 4,
    table.header([映射方式], [Tag位数], [索引位数], [块内偏移]),
    [直接映射], [7位], [9位（行索引）], [2位],
    [四路组相联], [9位], [7位（组索引）], [2位],
    [全相联], [16位], [无], [2位],
  )

  #figure(
    image("../assets/computer_constitution/problem3_design.png", width: 80%),
    caption: [主存地址格式设计对比],
  )
]

#pagebreak()

== 虚拟存储器

#definition[虚拟存储器概述][
  将主存和辅存统一编址，提供更大地址空间。

  - *页式虚拟存储器*：物理地址 = 页表[虚页号] + 页内偏移
  - *TLB（快表）*：页表的高速缓存，加速虚实地址转换
  - *段式/段页式*：按逻辑段划分 + 段内分页
]

=== 历年真题

#problem[2025春 · 填空第4题][
  主存 32 MB，Cache 16 KB，Cache 的一个块有 8 个字，一个字 32 位。现已知缓存采用八路组相联映射，问 ABCDEFH 映射到 Cache 中第几组？
]

#solution[
  - 块大小 = 8字 $times$ 4字节 = 32字节，块内偏移 = 5位
  - Cache 总行数 = 16KB / 32B = 512行
  - 八路组相联：每组8行，组数 = 512 / 8 = 64组，组索引 = 6位
  - ABCDEFH 二进制：1010 1011 1100 1101 1110 1111
  - 组索引 = 地址中[10:5]（跳过5位块内偏移）。取 6位组号 = 对应地址段
  - 需要确认地址位数和具体计算（回忆版数据可能不完整，考点是理解组相联映射的地址划分方法）
]

#problem[2019补考 · 选择第6题][
  DRAM 和 SRAM 的比较：哪个集成度高？哪个速度快？
]

#solution[
  - SRAM速度快，但集成度低（6管/位）、功耗大、价格高
  - DRAM集成度高（1管1电容/位）、价格低，但速度慢、需刷新
]

#problem[2019补考 · 选择第4题][
  使用虚拟存储器的目的是什么？答案：*增加主存的容量*（逻辑上扩大可用的地址空间）。
]

#problem[2019补考 · 选择第5题（RAID）][
  RAID 技术中，哪个的容错能力最高？A. RAID0 \ B. RAID1 \ C. RAID3 \ D. RAID5
]

#solution[
  答案：*B（RAID1）*。

  #table(
    columns: 4,
    table.header[级别][名称][容错][特点],
    [RAID0], [条带化], [*无*], [数据分散存储，速度快但无冗余],
    [RAID1], [镜像], [*最高*], [完全镜像，100%冗余，可靠但容量利用率50%],
    [RAID3], [专用校验盘], [中等], [一块专用校验盘，可恢复单盘故障],
    [RAID5], [分布式校验], [较高], [校验信息分布在各盘，常用折中方案],
  )
]

#problem[2019补考 · 填空第7题（寻址范围）][
  规定一个字有 32 位，现有 16MB 存储空间，问寻址范围是什么？
]

#solution[
  16MB = $16 times 2^20$ B = $2^24$ B。

  - *按字节寻址*：地址线 24 位，寻址范围 $0 tilde.op 2^24 - 1$。
  - *按字寻址*（32位字 = 4字节）：总字数 = $2^24 / 2^2 = 2^22$，寻址范围 $0 tilde.op 2^22 - 1$。

  #note[提醒][
    考试时若题目未明确说明按字还是按字节寻址，需注意题干中是否有"字长32位""按字寻址"等提示。
  ]
]

#problem[2025春 · 第三题第4小题（片选 / 存储器扩展）][
  现要求系统程序区 4 KB，用户程序区 8 KB。有 ROM 芯片（2K×4位，4K×4位，1K×8位）和 RAM 芯片（2K×4位，4K×4位，8K×4位）。
  (1) 给出可行的组合方案总数。
  (2) 任选一种方案，说明哪些芯片做了位扩展，哪些做了字扩展。
]

#solution[
  系统程序区 4KB $arrow.r$ 用 ROM；用户程序区 8KB $arrow.r$ 用 RAM。

  *位扩展*：增加数据位宽（如用两片 4位芯片拼成 8位）。
  *字扩展*：增加存储容量（如用多片 2K芯片拼成 8K）。

  具体方案：系统区 4KB（ROM）：选 4K×4位 两片做位扩展 → 4K×8位。用户区 8KB（RAM）：选 8K×4位 两片做位扩展 → 8K×8位。共4片。

  #note[核心思路][
    先确定容量需求（KB），再通过位扩展凑数据位宽（通常8位），通过字扩展凑容量。
  ]
]

#problem[2019补考 · 简答第2题（DRAM刷新）][
  动态 RAM 中的三种刷新方式是什么？（3分）对一个 64×64 存储空间的存储器，最大刷新间隔为 2ms，当采用异步刷新方式时，刷新一行的时间间隔是多少？（1分）
]

#solution[
  三种刷新方式：
  1. *集中刷新*：在 2ms 内集中完成全部刷新，刷新期间不能读写（有"死区"）
  2. *分散刷新*：每次读写后都刷新一行，无"死区"但刷新频率高
  3. *异步刷新*：在 2ms 内均匀分布刷新操作，兼顾两者优点

  64×64 存储器 = 64行。最大刷新间隔 2ms。异步刷新：每行刷新间隔 = 2ms / 64 = *31.25μs*。
]

#problem[2019补考 · 简答第4题][
  列出动态 RAM 和静态 RAM 的五个方面的区别。
]

#solution[
  #table(
    columns: 3,
    table.header[区别][SRAM][DRAM],
    [存储原理], [双稳态触发器（6管/位）], [电容充放电（1管1电容/位）],
    [集成度], [低], [*高*（约为SRAM的4倍）],
    [速度], [*快*], [慢],
    [功耗], [大（持续通电）], [小（但刷新也耗电）],
    [是否需要刷新], [*不需要*], [*需要*定期刷新（典型2ms）],
    [价格], [贵], [便宜],
    [典型用途], [Cache], [主存],
  )
]

#problem[2019补考 · 简答第3题（片选/存储器连接）][
  片选题，参考唐朔飞《计算机组成原理》例 4.1 和例 4.2。要求选出芯片类型及数量，不要求画CPU和存储器的连接图。
]

#solution[
  典型解法：
  1. 确定总容量和数据位宽
  2. 位扩展：确定需要几片芯片拼出所需数据位宽（如 4位→8位需2片）
  3. 字扩展：确定需要几组芯片拼出所需存储容量
  4. 片选信号：用高位地址线通过译码器产生片选信号

  建议复习唐朔飞教材例4.1和例4.2作为标准练习。
]

== 补充考点：汉明码（海明码）★

#note[说明][
  汉明码在 2025春和2019补考中均出现（共3道题），属于必考内容。汉明码用于*检错和纠错*，核心是构造校验位。
]

=== 汉明码编码（配偶原则）

#definition[汉明码编码步骤][
  设数据位为 $k$ 位，校验位为 $r$ 位，需满足 $2^r >= k + r + 1$。

  校验位位置：$P_1$在第1位，$P_2$在第2位，$P_4$在第4位，$P_8$在第8位...（$2^(i-1)$位）。

  *配偶原则*：每组校验位及其负责的数据位中，*"1"的个数为偶数*。

  各组负责的位：
  - $P_1$：负责第 1,3,5,7,9,11... 位（二进制表示中第1位为1的位）
  - $P_2$：负责第 2,3,6,7,10,11... 位（二进制表示中第2位为1的位）
  - $P_4$：负责第 4,5,6,7,12,13,14,15... 位（二进制表示中第3位为1的位）
]

#problem[2025春 · 填空第3题（汉明码编码）][
  已知原比特数据 1100，根据配偶原则，其汉明码是 \_\_\_\_\_\_。
]

#solution[
  $k = 4$ 位数据，需 $r$ 满足 $2^r >= 4+r+1$。$r=3$ 时 $2^3=8 >= 4+3+1=8$，正好。共7位。

  7位汉明码布局（数据位 $D_4 D_3 D_2 D_1$ 放在 3,5,6,7位）：
  $P_1$ $P_2$ $D_4$ $P_4$ $D_3$ $D_2$ $D_1$
  数据 1100：$D_4 = 1, D_3 = 1, D_2 = 0, D_1 = 0$

  计算校验位（配偶原则：组内1的个数为偶数）：
  - $P_1$（位1,3,5,7）：$P_1, D_4(=1), D_3(=1), D_1(=0)$ → $P_1 + 1 + 1 + 0 $ 需为偶数 → $P_1 = 0$
  - $P_2$（位2,3,6,7）：$P_2, D_4(=1), D_2(=0), D_1(=0)$ → $P_2 + 1 + 0 + 0$ 需为偶数 → $P_2 = 1$
  - $P_4$（位4,5,6,7）：$P_4, D_3(=1), D_2(=0), D_1(=0)$ → $P_4 + 1 + 0 + 0$ 需为偶数 → $P_4 = 1$

  汉明码：$P_1 P_2 D_4 P_4 D_3 D_2 D_1 = 0111100$

  答案：*0111100*。
]

#problem[2019补考 · 填空第5题（汉明码编码）][
  给长度四位的数据，按照配偶原则得出七位汉明码。
]

#solution[
  方法同上。4位数据需3位校验位，共7位。关键在于记住各组负责的位和配偶原则。
]

#problem[2019补考 · 填空第4题（汉明码纠错）][
  给一个七位的汉明码，配奇原则纠错，问欲传送的数据应该是什么？
]

#solution[
  *配奇原则*：每组中"1"的个数为*奇数*（区别于配偶的偶数）。

  纠错方法：
  1. 计算每个校验组的奇偶性
  2. 出错的组对应的校验位位置之和 = 出错的数据位位置
  3. 将该位取反即可纠错

  例如：若有 $P_1 P_2 P_4$ 校验位，$P_1$ 和 $P_4$ 组出错，则 $1+4=5$，第5位出错。
]

=== 汉明码应试提醒

#conclusion[汉明码速记][
  #table(
    columns: 3,
    table.header[项目][配偶原则][配奇原则],
    [规则], [组内1的个数为*偶数*], [组内1的个数为*奇数*],
    [2025春], [编码题（数据1100→汉明码0111100）], [未考],
    [2019补], [编码题（4位数据→7位汉明码）], [纠错题（给汉明码找错误位）],
    [$P_1$管], [位 1,3,5,7,9,11...], [同上],
    [$P_2$管], [位 2,3,6,7,10,11...], [同上],
    [$P_4$管], [位 4,5,6,7,12,13,14,15...], [同上],
    [纠错], [出错组号之和 = 出错位号], [同上],
  )
]

== 第六章应试提醒

#conclusion[第六章高频考点][
  #table(
    columns: 3,
    table.header[题型][典型考法][出现年份],
    [选择/填空], [SRAM vs DRAM特性对比], [2019补],
    [选择/填空], [Cache三种映射方式特点], [常考],
    [计算], [组相联Cache的组号/行号计算], [2025春],
    [概念], [局部性原理（时间/空间）], [常考],
    [概念], [虚拟存储器/TLB的作用], [2019补],
    [概念], [写直达 vs 写回 策略对比], [常考],
    [计算], [寻址范围计算（按字/按字节）], [2019补],
    [概念], [DRAM三种刷新方式（集中/分散/异步）], [2019补],
    [概念], [RAID各级别特点（0/1/3/5）], [2019补],
    [设计], [片选/存储器扩展（位扩展+字扩展）], [2025春, 2019补],
    [计算], [汉明码编码（配偶/配奇原则）], [2025春, 2019补×2],
    [纠错], [汉明码纠错（配奇原则，连续考2次）], [2019补],
  )
]

#pagebreak()
= 第七章 系统总线和输入输出系统

#introduction[总线分类与仲裁][I/O方式对比][中断与DMA]

== 7.1 系统总线

=== 总线基本概念（PPT第4页）

#definition[什么是总线][
  总线是连接各个部件的信息传输线。两个核心特点：*分时*和*共享*。

  为什么要用总线？方便计算机系统的集成、扩展和进化。总线遵循协议或标准。

  - *串行*：一次传1位（USB、SATA）
  - *并行*：一次传多位（PCI、内存总线）
  - *猝发传输*：一个总线周期内连续传输多个地址连续的数据
]
#figure(
  image("../assets/computer_constitution/ch7_总线连接-04.png", width: 85%),
  caption: [总线基本概念],
)
=== 总线分类

#definition[三类总线][
  #table(
    columns: 3,
    table.header[类型][连接范围][示例],
    [*片内总线*], [芯片内部], [CPU内部各部件之间的总线],
    [*系统总线*], [CPU、主存、I/O接口间], [数据/地址/控制总线],
    [*通信总线（外部总线）*], [计算机之间或计算机与外设间], [USB、PCIe、RS-232],
  )
]

=== 系统总线的三总线结构

#definition[三总线][
  - *数据总线*：双向，位数与机器字长相关
  - *地址总线*：单向，决定寻址范围（$n$ 位 $arrow.r$ $2^n$ 寻址范围）
  - *控制总线*：有出有入（存储器读/写、中断请求/确认、总线请求/允许等）
]

#figure(
  image("../assets/computer_constitution/ch7_总线连接-04.png", width: 85%),
  caption: [总线连接方式：共享总线 vs 点对点分散连接（PPT第4页）],
)

=== 总线仲裁

#definition[总线仲裁方式][
  当多个设备同时请求总线时，需仲裁：

  - *集中式仲裁*：链式查询（优先级由物理位置决定）、计数器定时查询（优先级可编程）、独立请求（每个设备独立请求线）
  - *分布式仲裁*：各设备自行判断优先级
]

=== 总线性能指标

#definition[总线性能][
  - 总线宽度：数据总线的位数
  - 总线频率：总线的工作频率
  - *总线带宽* = 总线宽度 $times$ 总线频率（字节/秒）

  例如：32位、33MHz的PCI总线，带宽 = 4B $times$ 33M = 132MB/s
]

== 输入输出系统

=== I/O设备与特性

#definition[I/O设备特性][
  - *异步性*：I/O设备速度远慢于CPU
  - *实时性*：部分设备有实时响应要求
  - *独立性*：设备类型多样，需统一接口
]

=== 四种I/O控制方式（核心考点）

#figure(
  table(
    columns: 5,
    table.header[方式][CPU参与度][数据传输速度][适用场景][关键特点],
    [*程序查询*], [CPU全程等待], [最慢], [简单系统], [CPU反复查询状态，"忙等"],
    [*程序中断*], [CPU启动后并行], [中等], [中低速外设], [外设准备好后发中断，CPU响应],
    [*DMA*], [CPU仅在开始/结束参与], [快], [高速批量传输], [DMA控制器接管总线，直接存储器访问],
    [*通道*], [CPU几乎不参与], [最快], [大型机], [通道处理器独立管理I/O],
  ),
  caption: [四种I/O控制方式对比（*必记*！）],
)

=== 程序中断方式

#definition[中断过程][
  1. 外设发出中断请求
  2. CPU响应中断（关中断、保护断点和现场）
  3. 执行中断服务程序
  4. 恢复现场、开中断、返回

  *中断向量表*：存放各中断服务程序的入口地址。

  *中断屏蔽*：通过屏蔽字控制中断优先级。某中断源的屏蔽字中为1的位对应的中断源*不能被屏蔽*（即同级或更高级中断仍可响应）。
]

=== DMA方式

#definition[DMA（Direct Memory Access）][
  DMA 控制器直接控制总线的使用权，在存储器和外设之间进行数据传送，CPU 只在开始和结束时参与。

  *DMA传送三阶段*：
  1. *预处理*：CPU 设置 DMA控制器（传送方向、起始地址、数据量）
  2. *数据传送*：DMA控制器接管总线，完成数据传送
  3. *后处理*：传送完毕，DMA向CPU发中断，CPU做善后处理

  *DMA与CPU共享总线的方式*：
  - 停止CPU访问（CPU交出总线）
  - *周期挪用（周期窃取）*：DMA窃取CPU不访问存储器的周期
  - DMA与CPU交替访问（分时复用总线）
]

== 历年真题选编（第七章）

=== 选择题

#problem[课堂习题 · 系统总线1][
  在系统总线的数据线上，不可能传输的是 \_\_\_\_\_\_

  #choices(
    [指令],
    [操作数],
    [提手（应答）信号],
  )
]

#solution[
  答案：*C*（提手/应答信号）。

  *系统总线的三个组成部分*：
  - *数据总线*：传输指令、操作数、地址等数据信息
  - *地址总线*：传输地址信息
  - *控制总线*：传输控制信号和握手信号（请求/应答）
  
  握手信号属于控制总线，而非数据线。
]

#problem[课堂习题 · 系统总线2][
  系统总线用来连接 \_\_\_\_\_\_

  #choices(
    [寄存器和运算器部件],
    [运算器和控制器部件],
    [CPU、主存和外设部件],
  )
]

#solution[
  答案：*C*。系统总线是连接 CPU、主存储器和 I/O 接口（外设）的主要通路。

  *总线分类回顾*：
  - *片内总线*：芯片内部各部件连接
  - *系统总线*：CPU、主存、I/O 接口间的连接
  - *通信总线*：计算机之间的连接
]

#problem[2025春 · 第4题][
  异步通信的特点是什么？\_\_\_\_\_\_

  #choices(
    [采用统一时钟，不采用握手],
    [不采用统一时钟，不采用握手],
    [不采用统一时钟，采用握手],
    [采用统一时钟，采用握手],
  )
]

#solution[
  答案：*C*。异步通信不依赖统一时钟，靠握手信号（请求/应答）同步。
  
  同步通信 = 统一时钟。异步通信 = 握手信号。半同步 = 时钟 + 握手。
]

#problem[2025春 · 第5题][
  DMA 执行过程中，并行性体现在 \_\_\_\_\_\_

  #choices(
    [多个外设可以并行地通过 DMA 传送数据],
    [数据传送和程序可以并行工作],
    [主存储器和 CPU 可以并行工作],
    [主存储器和外设可以并行工作],
  )
]

#solution[
  答案：*B*。DMA 传送期间，CPU 可继续执行其他程序（只要不访问同一存储器区域），实现数据传送与程序执行并行。
]

#problem[2025春 · 第7题][
  CPU 不能直接访问的是 \_\_\_\_\_\_

  #choices([寄存器], [硬盘], [主存], [Cache])
]

#solution[
  答案：*B*（硬盘）。CPU 能直接访问寄存器、主存、Cache。硬盘是辅助存储器，需通过 I/O 接口/控制器间接访问。
]

#problem[2025春 · 第8题][
  下列过程不会在中断时发生的包括 \_\_\_\_\_\_
  I. 保护现场 \ II. 指令入栈 \ III. DMA \ IV. 恢复现场 \ V. 关中断

  #choices([II, III, IV], [II, III, V], [III, V], [只有 III])
]

#solution[
  答案：*D*（只有 III，DMA）。

  中断过程：关中断 → 保护断点（PC入栈）→ 保护现场 → 执行中断服务 → 恢复现场 → 开中断 → 返回。DMA 是独立于中断的数据传送方式，不在中断流程中。
]

#problem[2025春 · 第9题][
  4 个中断源 1,2,3,4，原优先级顺序为 1-2-3-4，现在改为：1 的屏蔽字 1111，2 的屏蔽字 0111，3 的屏蔽字 0010，4 的屏蔽字 0011，优先级从高到低的顺序是 \_\_\_\_\_\_

  #choices([1-3-2-4], [1-2-3-4], [1-2-4-3], [3-4-2-1])
]

#solution[
  答案：*A*（1-3-2-4）。

  屏蔽字中某位为1表示*不能被该中断源屏蔽*（即该位对应的中断源优先级更高或同级）。

  - 1的屏蔽字 1111：不能被1,2,3,4屏蔽 $arrow.r$ 1优先级最高
  - 2的屏蔽字 0111：不能被2,3,4屏蔽，能被1屏蔽 $arrow.r$ 2优先级低于1
  - 3的屏蔽字 0010：不能被2屏蔽，能被1,3,4屏蔽 $arrow.r$ 3优先级低于1，高于2
  - 4的屏蔽字 0011：不能被3,4屏蔽，能被1,2屏蔽 $arrow.r$ 4优先级低于1,2，高于3

  其实更简单：某位=1表示"我能屏蔽他"或"我不能屏蔽他"的约定需确认。标准分析：1111 = 1不能被任何人屏蔽→1最高。0111 = 2不能被2/3/4屏蔽→2次高。0010 = 3只能被2屏蔽→但2屏蔽字0111不含3...综合得1-3-2-4。
]

#problem[2025春 · 第10题][
  在 DMA 周期窃取中，窃取的是 \_\_\_\_\_\_ 周期？

  #choices([存取], [指令], [CPU], [总线])
]

#solution[
  答案：*D*（总线）。DMA 周期窃取/周期挪用是在 CPU 不访问存储器时，DMA 控制器窃取*总线*的使用权来完成数据传送。
]

#problem[2019补考 · 选择第3题][
  医生将手伸出等护士递刀，握紧后护士才松手。相当于\_\_\_\_通信中的\_\_\_\_方式。

  #choices([异步], [全互锁])
]

#solution[
  答案：A：*异步*，B：*全互锁*。

  医生伸手（请求）→ 护士递刀（应答）→ 医生握紧（确认）→ 护士松手。这是异步通信的*全互锁*方式（发送方和接收方都有确认）。
]

#problem[2019补考 · 选择第2题][
  计数器定时查询的设备优先级 \_\_\_\_\_\_

  #choices([设备优先级一致], [序号越小，优先级越高])
]

#solution[
  答案：设备的优先级取决于计数器*当前计数值*，不是固定的。在通常实现中，计数器初始值为0，因此序号越小的设备越先被查询到。但严格来说计数器定时查询方式的优先级是*可编程*的。
]

=== 填空题

#problem[2019补考 · 填空第3题][
  总线通信的四种常见方式是什么？
]

#solution[
  *同步通信*、*异步通信*、*半同步通信*、*分离式通信*。
]

#problem[2019补考 · 填空第6题][
  DMA 数据传输的三个阶段是什么？
]

#solution[
  *预处理*、*数据传送*、*后处理*。
]

=== 简答题

#problem[2025春 · 第三题第3小题][
  简述 DMA 方式和程序中断方式的区别，至少回答出三个方面。
]

#solution[
  #table(
    columns: 3,
    table.header[区别维度][DMA方式][程序中断方式],
    [*数据传送方式*], [硬件直接传送（DMA控制器）], [CPU执行程序传送],
    [*CPU参与程度*], [仅在开始和结束时参与], [每条数据传送都需要CPU干预],
    [*响应时间*], [快（硬件级）], [慢（需中断响应+程序执行）],
    [*适用场景*], [高速批量数据传送], [中低速外设],
    [*对CPU的影响*], [CPU可继续执行程序（并行）], [CPU需暂停当前程序],
    [*中断请求时机*], [数据块传送完毕后], [每个数据准备好后],
  )
]

#problem[2019补考 · 简答第5题（屏蔽字控制）][
  利用屏蔽字来控制外设的执行顺序。参考唐朔飞教材 8.4.2 第40题和 8.4.3 第31~34题。
]

#solution[
  通过设置中断屏蔽字可以*动态改变中断优先级*，从而控制外设的响应顺序。

  原理：每个中断源的屏蔽字中某位为 1，表示该位对应的中断源*可以屏蔽*当前中断源。通过修改屏蔽字实现灵活的优先级调整。

  建议复习唐朔飞教材对应例题（屏蔽字变化后的优先级分析）。
]

== 补充考点：片选与存储器扩展

#problem[2025春 / 2019补（存储器扩展大题）][
  片选/存储器扩展是两届均考的大题。核心方法：
]

#solution[
  *三步法*：
  1. *确定需求*：总容量 + 数据位宽（通常 8 位）
  2. *位扩展*：单芯片位宽不足时多片并联（如 4位→8位需 2 片）
  3. *字扩展*：容量不足时多组并联，高位地址线经*译码器*产生*片选信号*

  *芯片规格读法*："2K×4位" = 2K 存储单元 $times$ 4 位数据宽度。

  *片选地址分配*：用最高几位地址线接译码器。如 16 根地址线 A0-A15，A13-A15 接 3-8译码器产生 8 个片选。
]

== 考试版RISC-V指令卡片

#for p in (1,2) {
  [
    #image(
      "../assets/computer_constitution/RISC-V-Reference-Data指令卡片-考试版.pdf",
      page: p,
      width: 110%
    )

    #pagebreak(weak:true)
]
}